% Blueprint content for "Primes of the Form x^2 + ny^2" (Cox, 2nd ed.).
%
% Each node carries \label, \lean (the matching Lean declaration), \uses (its
% dependencies in the DAG), and \leanok on every statement whose Lean signature
% typechecks. A node whose Lean proof is complete (no `sorry`, verified by
% `#print axioms`) additionally carries `\begin{proof}\leanok\end{proof}`, which
% recolors it "proved" in the dependency graph. Nodes still backed by a `sorry`
% proof carry no proof `\leanok` (Pass B is discharging these incrementally).
%
% See ROADMAP.md for the Mathlib audit of each node.

\chapter{Part I — From Fermat to Gauss}

This part is largely formalizable with current Mathlib (quadratic forms, \texttt{ZMod},
quadratic reciprocity). It develops the classical theory of binary quadratic forms,
their class group, and genus theory, culminating in congruence criteria for
representation by the principal form.

\section{Fermat, Euler, and quadratic reciprocity}

This section follows Cox \S1. The three Fermat theorems are obtained from Euler's
two-step strategy (Descent Step plus Reciprocity Step); the Reciprocity Step is
solved in general by quadratic reciprocity, packaged through the character
$\chi$ of Lemma 1.14 and Corollary 1.19.

\begin{theorem}[Two-square identity; Cox (1.3)]
  \label{eq:mul_sq_add_sq}
  \lean{PrimesXNY2.Fermat.mul_sq_add_sq}
  \leanok
  $(x^2+y^2)(z^2+w^2) = (xz - yw)^2 + (xw + yz)^2$.
\end{theorem}
\begin{proof}\leanok\end{proof}

\begin{theorem}[Identity for $x^2+ny^2$; Cox (1.6)]
  \label{eq:mul_sq_add_nsq}
  \lean{PrimesXNY2.Fermat.mul_sq_add_nsq}
  \leanok
  $(x^2+ny^2)(z^2+nw^2) = (xz - nyw)^2 + n(xw + yz)^2$.
\end{theorem}
\begin{proof}\leanok\end{proof}

\begin{theorem}[Cox Lemma 1.4]
  \label{lem:descent_lemma}
  \lean{PrimesXNY2.Fermat.descent_lemma}
  \leanok
  If $N$ is a sum of two relatively prime squares and a prime $q = x^2+y^2$
  divides $N$, then $N/q$ is again a sum of two relatively prime squares.
\end{theorem}
\begin{proof}
  \uses{eq:mul_sq_add_sq}
\end{proof}

\begin{theorem}[Descent Step, $n=1$; Cox \S1]
  \label{thm:descent}
  \lean{PrimesXNY2.Fermat.descent_step}
  \leanok
  If an odd prime $p$ divides $x^2+y^2$ with $\gcd(x,y)=1$, then $p$ is itself a
  sum of two squares.
\end{theorem}
\begin{proof}
  \uses{lem:descent_lemma}
  Repeated application of Lemma 1.4 (\ref{lem:descent_lemma}) descends to $p$.
\end{proof}

\begin{theorem}[Two squares; Cox Thm 1.2]
  \label{thm:fermat_two_squares}
  \lean{PrimesXNY2.Fermat.prime_sq_add_sq}
  \leanok
  An odd prime $p$ is a sum of two squares, $p = x^2 + y^2$, if and only if
  $p \equiv 1 \pmod 4$.
\end{theorem}
\begin{proof}
  \uses{thm:descent, thm:legendre_supplementary}
  Reciprocity shows $-1$ is a quadratic residue mod $p$ exactly when
  $p \equiv 1 \pmod 4$; descent (\ref{thm:descent}) then produces the
  representation.
\end{proof}

\begin{theorem}[$x^2 + 2y^2$]
  \label{thm:fermat_x2_2y2}
  \lean{PrimesXNY2.Fermat.prime_sq_add_two_sq}
  \leanok
  An odd prime $p$ satisfies $p = x^2 + 2y^2$ iff $p \equiv 1, 3 \pmod 8$.
\end{theorem}
\begin{proof}
  \uses{thm:descent, thm:legendre_supplementary}
\end{proof}

\begin{theorem}[$x^2 + 3y^2$]
  \label{thm:fermat_x2_3y2}
  \lean{PrimesXNY2.Fermat.prime_sq_add_three_sq}
  \leanok
  A prime $p > 3$ satisfies $p = x^2 + 3y^2$ iff $p \equiv 1 \pmod 3$.
\end{theorem}
\begin{proof}
  \uses{thm:descent, thm:quadratic_reciprocity}
\end{proof}

\begin{definition}[Legendre symbol]
  \label{def:legendre_symbol}
  \lean{legendreSym}
  \leanok
  For an odd prime $p$ and $a \in \mathbb Z$, the Legendre symbol $(a/p)$ is
  $0, 1, -1$ according as $p \mid a$, $a$ is a nonzero square, or a nonsquare mod
  $p$. (Provided by Mathlib.)
\end{definition}

\begin{theorem}[Cox Lemma 1.7]
  \label{lem:dvd_iff_isSquare}
  \lean{PrimesXNY2.Fermat.dvd_sq_add_nsq_iff_isSquare}
  \leanok
  For nonzero $n$ and an odd prime $p \nmid n$, $p$ divides a primitive value
  $x^2+ny^2$ iff $(-n/p) = 1$.
\end{theorem}
\begin{proof}
  \uses{def:legendre_symbol}
\end{proof}

\begin{theorem}[Euler's form of reciprocity; Cox Conjecture 1.9]
  \label{conj:euler_reciprocity}
  \lean{PrimesXNY2.Fermat.euler_reciprocity}
  \leanok
  For distinct odd primes $p,q$, $(q/p)=1$ iff $p \equiv \pm\beta^2 \pmod{4q}$ for
  some odd $\beta$.
\end{theorem}
\begin{proof}
  \uses{def:legendre_symbol}
\end{proof}

\begin{theorem}[Quadratic Reciprocity; Cox Proposition 1.10]
  \label{thm:quadratic_reciprocity}
  \lean{PrimesXNY2.Fermat.quadratic_reciprocity}
  \leanok
  For distinct odd primes $p,q$,
  $(p/q)(q/p) = (-1)^{\frac{p-1}{2}\cdot\frac{q-1}{2}}$.
\end{theorem}
\begin{proof}
  \uses{conj:euler_reciprocity, def:legendre_symbol}
\end{proof}

\begin{theorem}[Supplementary laws]
  \label{thm:legendre_supplementary}
  \lean{PrimesXNY2.Fermat.legendreSym_supplementary}
  \leanok
  $(-1/p) = (-1)^{(p-1)/2}$ and $(2/p) = (-1)^{(p^2-1)/8}$.
\end{theorem}
\begin{proof}
  \uses{def:legendre_symbol}
\end{proof}

\begin{theorem}[Cox Lemma 1.14]
  \label{lem:quadraticChar}
  \lean{PrimesXNY2.Fermat.exists_quadraticChar}
  \leanok
  For nonzero $D \equiv 0,1 \pmod 4$ there is a homomorphism
  $\chi : (\mathbb Z/D\mathbb Z)^\times \to \{\pm 1\}$ with $\chi([m]) = (D/m)$
  (Jacobi symbol) for odd $m$ prime to $D$.
\end{theorem}
\begin{proof}
  \uses{thm:quadratic_reciprocity, def:legendre_symbol}
\end{proof}

\begin{theorem}[Reciprocity Step solved; Cox Corollary 1.19]
  \label{thm:reciprocity_step}
  \lean{PrimesXNY2.Fermat.reciprocity_step}
  \leanok
  For each $n>0$ there is a set of residues mod $4n$ characterizing the odd
  primes $p \nmid n$ that divide a primitive $x^2+ny^2$.
\end{theorem}
\begin{proof}
  \uses{lem:dvd_iff_isSquare, lem:quadraticChar}
\end{proof}

\subsection{Exercises}

\begin{theorem}[Exercise 1.1(a)]
  \label{exr:s1_1_a}
  \lean{PrimesXNY2.PartI.S1.ex_1_1_a}
  \leanok
  Prove the identity (1.3).
\end{theorem}
\begin{proof}\uses{eq:mul_sq_add_sq}\end{proof}

\begin{theorem}[Exercise 1.1(b)]
  \label{exr:s1_1_b}
  \lean{PrimesXNY2.PartI.S1.ex_1_1_b}
  \leanok
  Euler's generalization
  $(ax^2+cy^2)(az^2+cw^2) = (axz - cyw)^2 + ac(xw + yz)^2$.
\end{theorem}
\begin{proof}\uses{eq:mul_sq_add_nsq}\end{proof}

\begin{theorem}[Exercise 1.2(a)]
  \label{exr:s1_2_a}
  \lean{PrimesXNY2.PartI.S1.ex_1_2_a}
  \leanok
  For $k \ge 1$, $\Delta^k$ applied to $g$ is an integral linear combination of
  $g(x), \dots, g(x+k)$.
\end{theorem}

\begin{theorem}[Exercise 1.2(b)]
  \label{exr:s1_2_b}
  \lean{PrimesXNY2.PartI.S1.ex_1_2_b}
  \leanok
  For a monic $f$ of degree $d$, $\Delta^d f = d!$.
\end{theorem}

\begin{theorem}[Exercise 1.2(c)]
  \label{exr:s1_2_c}
  \lean{PrimesXNY2.PartI.S1.ex_1_2_c}
  \leanok
  A monic integer polynomial of degree $< p$ is not identically $0 \bmod p$.
\end{theorem}
\begin{proof}\uses{exr:s1_2_a, exr:s1_2_b}\end{proof}

\begin{theorem}[Exercise 1.3(a)]
  \label{exr:s1_3_a}
  \lean{PrimesXNY2.PartI.S1.ex_1_3_a}
  \leanok
  Lemma 1.4 for the form $x^2+ny^2$.
\end{theorem}
\begin{proof}\uses{lem:descent_lemma}\end{proof}

\begin{theorem}[Exercise 1.3(b)]
  \label{exr:s1_3_b}
  \lean{PrimesXNY2.PartI.S1.ex_1_3_b}
  \leanok
  The descent works for $n=3$, $q=4$.
\end{theorem}
\begin{proof}\uses{exr:s1_3_a}\end{proof}

\begin{theorem}[Exercise 1.4(a)]
  \label{exr:s1_4_a}
  \lean{PrimesXNY2.PartI.S1.ex_1_4_a}
  \leanok
  Descent Step for $x^2+2y^2$.
\end{theorem}
\begin{proof}\uses{exr:s1_3_a}\end{proof}

\begin{theorem}[Exercise 1.4(b)]
  \label{exr:s1_4_b}
  \lean{PrimesXNY2.PartI.S1.ex_1_4_b}
  \leanok
  Descent Step for $x^2+3y^2$ (odd $p$).
\end{theorem}
\begin{proof}\uses{exr:s1_3_b}\end{proof}

\begin{theorem}[Exercise 1.5]
  \label{exr:s1_5}
  \lean{PrimesXNY2.PartI.S1.ex_1_5}
  \leanok
  If $p = 3k+1$ is prime then $(-3/p) = 1$.
\end{theorem}

\begin{theorem}[Exercise 1.6]
  \label{exr:s1_6}
  \lean{PrimesXNY2.PartI.S1.ex_1_6}
  \leanok
  Prove Lemma 1.7.
\end{theorem}
\begin{proof}\uses{lem:dvd_iff_isSquare}\end{proof}

\begin{theorem}[Exercise 1.7]
  \label{exr:s1_7}
  \lean{PrimesXNY2.PartI.S1.ex_1_7}
  \leanok
  Quadratic reciprocity in the form (1.12): $(p^*/q) = (q/p)$.
\end{theorem}
\begin{proof}\uses{thm:quadratic_reciprocity}\end{proof}

\begin{theorem}[Exercise 1.8]
  \label{exr:s1_8}
  \lean{PrimesXNY2.PartI.S1.ex_1_8}
  \leanok
  The reciprocity statement (1.13).
\end{theorem}
\begin{proof}\uses{conj:euler_reciprocity}\end{proof}

\begin{theorem}[Exercise 1.9(a)]
  \label{exr:s1_9_a}
  \lean{PrimesXNY2.PartI.S1.ex_1_9_a}
  \leanok
  For $p>3$: $(-3/p)=1 \iff p \equiv 1 \pmod 3$.
\end{theorem}
\begin{proof}\uses{lem:dvd_iff_isSquare}\end{proof}

\begin{theorem}[Exercise 1.9(b)]
  \label{exr:s1_9_b}
  \lean{PrimesXNY2.PartI.S1.ex_1_9_b}
  \leanok
  The cases $x^2+y^2$, $x^2+2y^2$ are the supplementary laws.
\end{theorem}
\begin{proof}\uses{thm:legendre_supplementary}\end{proof}

\begin{theorem}[Exercise 1.10(a)]
  \label{exr:s1_10_a}
  \lean{PrimesXNY2.PartI.S1.ex_1_10_a}
  \leanok
  $(M/m) = (N/m)$ when $M \equiv N \pmod m$.
\end{theorem}

\begin{theorem}[Exercise 1.10(b)]
  \label{exr:s1_10_b}
  \lean{PrimesXNY2.PartI.S1.ex_1_10_b}
  \leanok
  Multiplicativity of the Jacobi symbol (1.15).
\end{theorem}

\begin{theorem}[Exercise 1.10(c)]
  \label{exr:s1_10_c}
  \lean{PrimesXNY2.PartI.S1.ex_1_10_c}
  \leanok
  The supplementary laws (1.16) for the Jacobi symbol.
\end{theorem}

\begin{theorem}[Exercise 1.10(d)]
  \label{exr:s1_10_d}
  \lean{PrimesXNY2.PartI.S1.ex_1_10_d}
  \leanok
  If $M$ is a quadratic residue mod $m$ then $(M/m)=1$ (converse false).
\end{theorem}

\begin{theorem}[Exercise 1.11]
  \label{exr:s1_11}
  \lean{PrimesXNY2.PartI.S1.ex_1_11}
  \leanok
  Completion of (1.17): $(D/m) = (D/n)$ for odd $m \equiv n \pmod D$.
\end{theorem}
\begin{proof}\uses{exr:s1_10_b, exr:s1_10_c}\end{proof}

\begin{theorem}[Exercise 1.12(a)]
  \label{exr:s1_12_a}
  \lean{PrimesXNY2.PartI.S1.ex_1_12_a}
  \leanok
  $\chi([m]) = (D/m)$ is a well-defined homomorphism.
\end{theorem}
\begin{proof}\uses{lem:quadraticChar}\end{proof}

\begin{theorem}[Exercise 1.12(b)]
  \label{exr:s1_12_b}
  \notready
  $\chi([-1]) = 1$ if $D > 0$ and $-1$ if $D < 0$. \emph{UNVERIFIED: a faithful
  self-contained statement must fix the specific $\chi$ of Lemma 1.14; flagged.}
\end{theorem}

\begin{theorem}[Exercise 1.12(c)]
  \label{exr:s1_12_c}
  \notready
  Value of $\chi([2])$ when $D \equiv 1 \pmod 4$. \emph{UNVERIFIED: same reason as
  1.12(b); flagged.}
\end{theorem}

\begin{theorem}[Exercise 1.13(a)]
  \label{exr:s1_13_a}
  \lean{PrimesXNY2.PartI.S1.ex_1_13_a}
  \leanok
  Quadratic reciprocity, assuming Lemma 1.14.
\end{theorem}
\begin{proof}\uses{lem:quadraticChar}\end{proof}

\begin{theorem}[Exercise 1.13(b)]
  \label{exr:s1_13_b}
  \lean{PrimesXNY2.PartI.S1.ex_1_13_b}
  \leanok
  The supplementary laws, assuming Lemma 1.14.
\end{theorem}
\begin{proof}\uses{lem:quadraticChar}\end{proof}

\begin{theorem}[Exercise 1.14]
  \label{exr:s1_14}
  \lean{PrimesXNY2.PartI.S1.ex_1_14}
  \leanok
  When $n \equiv 3 \pmod 4$ the Reciprocity Step congruence can be taken mod $n$.
\end{theorem}
\begin{proof}\uses{thm:reciprocity_step}\end{proof}

\begin{theorem}[Exercise 1.15]
  \label{exr:s1_15}
  \lean{PrimesXNY2.PartI.S1.ex_1_15}
  \leanok
  The residue classes in $(\mathbb Z/84\mathbb Z)^\times$ with $(-21/p)=1$.
\end{theorem}
\begin{proof}\uses{thm:quadratic_reciprocity}\end{proof}

\begin{theorem}[Exercise 1.16]
  \label{exr:s1_16}
  \notready
  When $D = 4q$ ($q$ an odd prime), properties (i) and (ii) suffice to determine
  $K = \ker\chi$. \emph{UNVERIFIED: a faithful statement of the uniqueness
  characterization of the index-two subgroup is delicate; flagged.}
\end{theorem}

\section{Binary quadratic forms and reduction}

\begin{definition}[Binary quadratic form]
  \label{def:binary_qf}
  \lean{PrimesXNY2.Forms.BinaryQF}
  \leanok
  An integral binary quadratic form $f(x,y) = a x^2 + b x y + c y^2$, recorded by
  its coefficient triple $(a,b,c)$.
\end{definition}

\begin{definition}[Discriminant]
  \label{def:discriminant}
  \lean{PrimesXNY2.Forms.BinaryQF.discr}
  \leanok
  \uses{def:binary_qf}
  The discriminant of $f = (a,b,c)$ is $D = b^2 - 4ac$.
\end{definition}

\begin{definition}[Proper equivalence]
  \label{def:proper_equiv}
  \lean{PrimesXNY2.Forms.ProperlyEquivalent}
  \leanok
  \uses{def:binary_qf}
  Two forms are properly equivalent if related by a determinant-one integer change
  of variables, i.e.\ by an element of $\mathrm{SL}_2(\mathbb Z)$.
\end{definition}

\begin{definition}[Reduced form]
  \label{def:reduced}
  \lean{PrimesXNY2.Forms.BinaryQF.Reduced}
  \leanok
  \uses{def:binary_qf, def:discriminant}
  A positive definite form $(a,b,c)$ is reduced when $|b| \le a \le c$, with
  $b \ge 0$ whenever $|b| = a$ or $a = c$.
\end{definition}

\begin{theorem}[Reduction theorem; Cox Thm 2.8]
  \label{thm:reduction}
  \lean{PrimesXNY2.Forms.exists_unique_reduced}
  \leanok
  Every positive definite form is properly equivalent to a unique reduced form.
\end{theorem}
\begin{proof}
  \uses{def:reduced, def:proper_equiv}
\end{proof}

\begin{theorem}[Finiteness of the class number; Cox Thm 2.13]
  \label{thm:finite_class_number}
  \lean{PrimesXNY2.Forms.finite_reduced_of_discr}
  \leanok
  For each $D < 0$ there are only finitely many reduced forms of discriminant $D$.
\end{theorem}
\begin{proof}
  \uses{thm:reduction}
\end{proof}

\begin{definition}[Form evaluation]
  \label{def:form_eval}
  \lean{PrimesXNY2.Forms.BinaryQF.eval}
  \leanok
  \uses{def:binary_qf}
  $f(x,y) = a x^2 + b x y + c y^2$ as a function of $(x,y)$.
\end{definition}

\begin{definition}[Primitive form]
  \label{def:primitive}
  \lean{PrimesXNY2.Forms.BinaryQF.Primitive}
  \leanok
  \uses{def:binary_qf}
  A form is primitive when $\gcd(a,b,c)=1$.
\end{definition}

\begin{definition}[Positive definite]
  \label{def:posdef}
  \lean{PrimesXNY2.Forms.BinaryQF.PosDef}
  \leanok
  \uses{def:discriminant}
  $a > 0$ and $D < 0$.
\end{definition}

\begin{definition}[Indefinite]
  \label{def:indefinite}
  \lean{PrimesXNY2.Forms.BinaryQF.Indefinite}
  \leanok
  \uses{def:discriminant}
  $D > 0$.
\end{definition}

\begin{definition}[Action of an integer matrix]
  \label{def:action}
  \lean{PrimesXNY2.Forms.action}
  \leanok
  \uses{def:binary_qf}
  The change of variables $(x,y) \mapsto (px+qy, rx+sy)$ by a matrix $M$.
\end{definition}

\begin{definition}[Full equivalence; Cox (2.2)]
  \label{def:equivalent}
  \lean{PrimesXNY2.Forms.Equivalent}
  \leanok
  \uses{def:action}
  Forms related by a change of variables of determinant $\pm 1$
  ($\mathrm{GL}_2(\mathbb Z)$).
\end{definition}

\begin{theorem}[Proper equivalence is an equivalence relation]
  \label{thm:proper_equiv_equivalence}
  \lean{PrimesXNY2.Forms.properlyEquivalent_equivalence}
  \leanok
  \uses{def:proper_equiv}
\end{theorem}

\begin{theorem}[Proper equivalence preserves the discriminant]
  \label{thm:discr_invariant}
  \lean{PrimesXNY2.Forms.discr_eq_of_properlyEquivalent}
  \leanok
  \uses{def:proper_equiv, def:discriminant}
\end{theorem}

\begin{theorem}[Cox (2.4)]
  \label{eq:four_mul_eval}
  \lean{PrimesXNY2.Forms.four_mul_eval}
  \leanok
  \uses{def:form_eval, def:discriminant}
  $4a\,f(x,y) = (2ax+by)^2 - D y^2$.
\end{theorem}
\begin{proof}\leanok\end{proof}

\begin{definition}[Principal form]
  \label{def:principal_form}
  \lean{PrimesXNY2.Forms.principalForm}
  \leanok
  \uses{def:discriminant}
  $x^2 - (D/4)y^2$ if $D \equiv 0$, else $x^2 + xy + ((1-D)/4)y^2$.
\end{definition}

\begin{theorem}[Cox Lemma 2.3]
  \label{lem:repr_iff_equiv}
  \lean{PrimesXNY2.Genus.properlyRepresents_iff_properlyEquivalent}
  \leanok
  \uses{def:represents, def:proper_equiv}
  $f$ properly represents $m$ iff $f \sim [m,b,c]$ for some $b,c$.
\end{theorem}

\begin{theorem}[Cox Lemma 2.5]
  \label{lem:repr_iff_isSquare_general}
  \lean{PrimesXNY2.Genus.properlyRepresents_iff_isSquare_general}
  \leanok
  \uses{lem:repr_iff_equiv, def:primitive}
  For $D \equiv 0,1 \pmod 4$ and odd $m$ prime to $D$, $m$ is properly represented
  by a primitive form of discriminant $D$ iff $D$ is a square mod $m$.
\end{theorem}

\begin{theorem}[Cox Corollary 2.6]
  \label{cor:c2_6}
  \lean{PrimesXNY2.Genus.cor_2_6}
  \leanok
  \uses{lem:repr_iff_isSquare_general}
  For an odd prime $p \nmid n$, $(-n/p)=1$ iff $p$ is represented by a primitive
  form of discriminant $-4n$.
\end{theorem}

\begin{theorem}[Cox Proposition 2.15]
  \label{prop:p2_15}
  \lean{PrimesXNY2.Genus.prop_2_15}
  \leanok
  \uses{cor:c2_6, thm:reduction}
  For an odd prime $p \nmid n$, $(-n/p)=1$ iff $p$ is represented by one of the
  reduced forms of discriminant $-4n$.
\end{theorem}

\begin{theorem}[Cox Theorem 2.16]
  \label{thm:t2_16}
  \lean{PrimesXNY2.Genus.thm_2_16}
  \leanok
  \uses{prop:p2_15, lem:quadraticChar}
  For negative $D \equiv 0,1 \pmod 4$ and odd prime $p \nmid D$, $(D/p)=1$
  (i.e.\ $[p] \in \ker\chi$) iff $p$ is represented by a reduced form of disc $D$.
\end{theorem}

\begin{theorem}[Composition identity (2.30)]
  \label{eq:comp_2_30}
  \lean{PrimesXNY2.Forms.comp_identity_2_30}
  \leanok
\end{theorem}
\begin{proof}\leanok\end{proof}

\begin{theorem}[Composition identity (2.31)]
  \label{eq:comp_2_31}
  \lean{PrimesXNY2.Forms.comp_identity_2_31}
  \leanok
\end{theorem}
\begin{proof}\leanok\end{proof}

\begin{theorem}[Landau; Cox Theorem 2.18]
  \label{thm:landau}
  \lean{PrimesXNY2.Forms.class_number_one}
  \leanok
  \uses{def:reduced, def:primitive}
  $h(-4n)=1$ iff $n \in \{1,2,3,4,7\}$.
\end{theorem}

\section{The form class group and Dirichlet composition}

\begin{definition}[Form class group]
  \label{def:form_class_group}
  \lean{PrimesXNY2.Forms.FormClassGroup}
  \leanok
  \uses{def:proper_equiv, def:discriminant}
  $C(D)$ is the set of proper equivalence classes of forms of discriminant $D$.
\end{definition}

\begin{definition}[Dirichlet composition]
  \label{def:dirichlet_composition}
  \lean{PrimesXNY2.Forms.compose}
  \leanok
  \uses{def:form_class_group}
  The group law on $C(D)$ given by Dirichlet composition of forms.
\end{definition}

\begin{definition}[Opposite form]
  \label{def:opposite_form}
  \lean{PrimesXNY2.Forms.BinaryQF.opposite}
  \leanok
  \uses{def:binary_qf}
  The opposite of $a x^2 + b x y + c y^2$ is $a x^2 - b x y + c y^2$.
\end{definition}

\begin{lemma}[Cox Lemma 3.2]
  \label{lem:l3_2}
  \lean{PrimesXNY2.Forms.lemma_3_2}
  \leanok
  \uses{def:discriminant}
  For forms of discriminant $D$ with $\gcd(a,a',(b+b')/2)=1$, there is a $B$, unique
  mod $2aa'$, with $B \equiv b\ (2a)$, $B \equiv b'\ (2a')$, $B^2 \equiv D\ (4aa')$.
\end{lemma}

\begin{lemma}[Cox Lemma 3.5]
  \label{lem:l3_5}
  \lean{PrimesXNY2.Forms.lemma_3_5}
  \leanok
  The congruences $p_i B \equiv q_i\ (m)$ (with $\gcd(p_1,\dots,p_r,m)=1$) are
  solvable iff $p_i q_j \equiv p_j q_i\ (m)$ for all $i,j$.
\end{lemma}

\begin{definition}[Dirichlet composition formula; Cox (3.7)]
  \label{def:dirichlet_form}
  \lean{PrimesXNY2.Forms.dirichletForm}
  \leanok
  \uses{lem:l3_2}
  The composed form $a a' x^2 + B x y + \tfrac{B^2-D}{4aa'} y^2$, with $B$ from
  Lemma 3.2.
\end{definition}

\begin{proposition}[Cox Proposition 3.8]
  \label{prop:p3_8}
  \lean{PrimesXNY2.Forms.prop_3_8}
  \leanok
  \uses{def:dirichlet_form, def:primitive}
  The Dirichlet composition of two primitive positive definite forms of
  discriminant $D$ is again primitive positive definite of discriminant $D$.
\end{proposition}

\begin{theorem}[$C(D)$ is a finite abelian group; Cox Thm 3.9]
  \label{thm:form_class_group_is_group}
  \lean{PrimesXNY2.Forms.isCommGroup}
  \leanok
  \uses{def:dirichlet_composition}
  For $D<0$, Dirichlet composition is associative and commutative, the principal
  class is an identity, and every class has an inverse. (Faithful replacement of
  the earlier $\mathrm{Nonempty}(\mathrm{CommGroup}\,C(D))$, which holds for any
  nonempty type and was therefore vacuous.)
\end{theorem}
\begin{proof}
  \uses{prop:p3_8, thm:reduction}
\end{proof}

\begin{theorem}[Inverse is the opposite class; Cox Thm 3.9]
  \label{thm:t3_9_inverse}
  \lean{PrimesXNY2.Forms.thm_3_9_inverse}
  \leanok
  \uses{def:opposite_form, def:dirichlet_composition}
  The inverse of the class of $f$ is the class of its opposite form.
\end{theorem}

\begin{lemma}[Cox Lemma 3.10]
  \label{lem:l3_10}
  \lean{PrimesXNY2.Forms.lemma_3_10}
  \leanok
  \uses{thm:form_class_group_is_group, def:reduced}
  A reduced form has order $\le 2$ in $C(D)$ iff $b=0$, $a=b$, or $a=c$.
\end{lemma}

\begin{definition}[The exponent $\mu$; Cox Prop 3.11]
  \label{def:mu}
  \lean{PrimesXNY2.Forms.mu}
  \leanok
  The number of assigned characters, from the count $r$ of odd primes dividing $D$
  and the value of $D \bmod 8$.
\end{definition}

\begin{proposition}[Cox Proposition 3.11]
  \label{prop:p3_11}
  \lean{PrimesXNY2.Forms.prop_3_11}
  \leanok
  \uses{def:mu, thm:form_class_group_is_group}
  $C(D)$ has exactly $2^{\mu-1}$ elements of order $\le 2$.
\end{proposition}

\section{Genus theory and representation}

\begin{definition}[Proper representation]
  \label{def:represents}
  \lean{PrimesXNY2.Genus.ProperlyRepresents}
  \leanok
  \uses{def:binary_qf}
  $f$ properly represents $m$ if $m = f(x,y)$ with $\gcd(x,y) = 1$.
\end{definition}

\begin{definition}[Genus]
  \label{def:genus}
  \lean{PrimesXNY2.Genus.genus}
  \leanok
  \uses{def:form_class_group}
  The genus of a form: the residues mod $D$ it represents, constant on each genus.
\end{definition}

\begin{definition}[Principal genus]
  \label{def:principal_genus}
  \lean{PrimesXNY2.Genus.principalGenus}
  \leanok
  \uses{def:genus}
  The genus of the principal form, equivalently the subgroup of squares in $C(D)$.
\end{definition}

\begin{theorem}[Cox Lemma 2.5, prime case]
  \label{thm:representation_residue}
  \lean{PrimesXNY2.Genus.properlyRepresents_iff_isSquare}
  \leanok
  \uses{def:represents}
  A prime $p \nmid D$ is properly represented by some form of discriminant $D$ iff
  $D$ is a quadratic residue mod $p$. (Prime case of Lemma 2.5,
  \ref{lem:repr_iff_isSquare_general}.)
\end{theorem}

\begin{theorem}[Cox Lemma 2.24]
  \label{lem:l2_24}
  \lean{PrimesXNY2.Genus.principalForm_values_subgroup}
  \leanok
  \uses{def:principal_form, def:genus}
  For negative $D \equiv 0,1 \pmod 4$, the residues in
  $(\mathbb Z/D\mathbb Z)^\times$ represented by the principal form form a
  subgroup $H$.
\end{theorem}

\begin{theorem}[Cox Lemma 2.25]
  \label{lem:l2_25}
  \lean{PrimesXNY2.Genus.properlyRepresents_coprime}
  \leanok
  \uses{def:form_eval}
  Every form properly represents some value relatively prime to a given $M$.
\end{theorem}

\begin{theorem}[Cox Theorem 2.26]
  \label{thm:t2_26}
  \lean{PrimesXNY2.Genus.thm_2_26}
  \leanok
  \uses{lem:l2_24, def:genus, thm:reduction}
  An odd prime $p \nmid D$ lies in the genus of $g$ iff $p$ is represented by a
  reduced form of disc $D$ in the same genus.
\end{theorem}

\begin{theorem}[Cox Corollary 2.27]
  \label{thm:genus_congruence}
  \lean{PrimesXNY2.Genus.represents_principal_iff_congruence}
  \leanok
  \uses{def:principal_genus, lem:l2_24}
  For $n>0$ and odd prime $p \nmid n$, $p$ is represented by a form of disc $-4n$
  in the principal genus iff $p \equiv \beta^2$ or $\beta^2 + n \pmod{4n}$.
  (Replaces an earlier vacuous/false statement.)
\end{theorem}

\subsection{Exercises}

\begin{theorem}[Exercise 2.1]
  \label{exr:s2_1}
  \lean{PrimesXNY2.PartI.S2.ex_2_1}
  \leanok
  Any represented value is $d^2$ times a properly represented value.
\end{theorem}
\begin{proof}\uses{def:represents}\end{proof}

\begin{theorem}[Exercise 2.2(a)]
  \label{exr:s2_2_a}
  \lean{PrimesXNY2.PartI.S2.ex_2_2_a}
  \leanok
  Equivalence and proper equivalence are equivalence relations.
\end{theorem}
\begin{proof}\uses{def:equivalent, thm:proper_equiv_equivalence}\end{proof}

\begin{theorem}[Exercise 2.2(b)]
  \label{exr:s2_2_b}
  \lean{PrimesXNY2.PartI.S2.ex_2_2_b}
  \leanok
  Improper equivalence is not an equivalence relation.
\end{theorem}
\begin{proof}\uses{def:action}\end{proof}

\begin{theorem}[Exercise 2.2(c)]
  \label{exr:s2_2_c}
  \lean{PrimesXNY2.PartI.S2.ex_2_2_c}
  \leanok
  Equivalent forms represent the same numbers.
\end{theorem}
\begin{proof}\uses{def:equivalent}\end{proof}

\begin{theorem}[Exercise 2.2(d)]
  \label{exr:s2_2_d}
  \lean{PrimesXNY2.PartI.S2.ex_2_2_d}
  \leanok
  A form equivalent to a primitive form is primitive.
\end{theorem}
\begin{proof}\uses{def:equivalent, def:primitive}\end{proof}

\begin{theorem}[Exercise 2.3]
  \label{exr:s2_3}
  \lean{PrimesXNY2.PartI.S2.ex_2_3}
  \leanok
  $D_f = (\det M)^2 D_g$ under a change of variables.
\end{theorem}
\begin{proof}\uses{def:action, def:discriminant}\end{proof}

\begin{theorem}[Exercise 2.4(a)]
  \label{exr:s2_4_a}
  \lean{PrimesXNY2.PartI.S2.ex_2_4_a}
  \leanok
  A form of positive discriminant represents both signs.
\end{theorem}
\begin{proof}\uses{eq:four_mul_eval}\end{proof}

\begin{theorem}[Exercise 2.4(b)]
  \label{exr:s2_4_b}
  \lean{PrimesXNY2.PartI.S2.ex_2_4_b}
  \leanok
  A form of negative discriminant is definite (sign of $a$).
\end{theorem}
\begin{proof}\uses{eq:four_mul_eval}\end{proof}

\begin{theorem}[Exercise 2.5]
  \label{exr:s2_5}
  \lean{PrimesXNY2.PartI.S2.ex_2_5}
  \leanok
  Corollary 2.6 for arbitrary discriminant.
\end{theorem}
\begin{proof}\uses{cor:c2_6}\end{proof}

\begin{theorem}[Exercise 2.6]
  \label{exr:s2_6}
  \lean{PrimesXNY2.PartI.S2.ex_2_6}
  \leanok
  A reduced form properly equivalent to $126x^2+74xy+13y^2$ exists.
\end{theorem}
\begin{proof}\uses{thm:reduction}\end{proof}

\begin{theorem}[Exercise 2.7]
  \label{exr:s2_7}
  \lean{PrimesXNY2.PartI.S2.ex_2_7}
  \leanok
  The bound (2.9).
\end{theorem}
\begin{proof}\uses{def:reduced}\end{proof}

\begin{theorem}[Exercise 2.8(a)]
  \label{exr:s2_8_a}
  \lean{PrimesXNY2.PartI.S2.ex_2_8_a}
  \leanok
  The minima (2.11) of a reduced form.
\end{theorem}
\begin{proof}\uses{def:reduced}\end{proof}

\begin{theorem}[Exercise 2.8(b)]
  \label{exr:s2_8_b}
  \lean{PrimesXNY2.PartI.S2.ex_2_8_b}
  \leanok
  Uniqueness part of Theorem 2.8 (exceptional cases).
\end{theorem}
\begin{proof}\uses{thm:reduction}\end{proof}

\begin{theorem}[Exercise 2.9(a)]
  \label{exr:s2_9_a}
  \notready
  Verify the table (2.14) of class numbers and reduced forms. \emph{UNVERIFIED:
  computational verification of a table, not a single proposition; flagged.}
\end{theorem}

\begin{theorem}[Exercise 2.9(b)]
  \label{exr:s2_9_b}
  \notready
  Compute the reduced forms of discriminants $-3,-15,-24,-31,-52$. \emph{UNVERIFIED:
  a per-discriminant enumeration, not a single faithfully-stateable proposition;
  flagged.}
\end{theorem}

\begin{theorem}[Exercise 2.10(a)]
  \label{exr:s2_10_a}
  \lean{PrimesXNY2.PartI.S2.ex_2_10_a}
  \leanok
  Reduction for indefinite forms.
\end{theorem}
\begin{proof}\uses{def:indefinite, thm:reduction}\end{proof}

\begin{theorem}[Exercise 2.10(b)]
  \label{exr:s2_10_b}
  \lean{PrimesXNY2.PartI.S2.ex_2_10_b}
  \leanok
  Bound $4a^2 \le D$.
\end{theorem}
\begin{proof}\uses{exr:s2_10_a}\end{proof}

\begin{theorem}[Exercise 2.10(c)]
  \label{exr:s2_10_c}
  \lean{PrimesXNY2.PartI.S2.ex_2_10_c}
  \leanok
  Finiteness of the indefinite class number.
\end{theorem}
\begin{proof}\uses{exr:s2_10_b}\end{proof}

\begin{theorem}[Exercise 2.11]
  \label{exr:s2_11}
  \lean{PrimesXNY2.PartI.S2.ex_2_11}
  \leanok
  The result (2.17) for $x^2+7y^2$.
\end{theorem}
\begin{proof}\uses{thm:t2_16, thm:quadratic_reciprocity}\end{proof}

\begin{theorem}[Exercise 2.12(a)]
  \label{exr:s2_12_a}
  \lean{PrimesXNY2.PartI.S2.ex_2_12_a}
  \leanok
  Non-prime-power factorization.
\end{theorem}

\begin{theorem}[Exercise 2.12(b)]
  \label{exr:s2_12_b}
  \lean{PrimesXNY2.PartI.S2.ex_2_12_b}
  \leanok
  $h(-32)=2$ and $h(-124)=3$.
\end{theorem}
\begin{proof}\uses{def:reduced}\end{proof}

\begin{theorem}[Exercise 2.13]
  \label{exr:s2_13}
  \lean{PrimesXNY2.PartI.S2.ex_2_13}
  \leanok
  The result (2.19) for $x^2+5y^2$.
\end{theorem}
\begin{proof}\uses{thm:t2_16, thm:quadratic_reciprocity}\end{proof}

\begin{theorem}[Exercise 2.14]
  \label{exr:s2_14}
  \lean{PrimesXNY2.PartI.S2.ex_2_14}
  \leanok
  The residues (2.20) of $x^2+5y^2$ and $2x^2+2xy+3y^2$.
\end{theorem}
\begin{proof}\uses{lem:l2_24}\end{proof}

\begin{theorem}[Exercise 2.15]
  \label{exr:s2_15}
  \lean{PrimesXNY2.PartI.S2.ex_2_15}
  \leanok
  The result (2.23) for $x^2+14y^2$, $2x^2+7y^2$.
\end{theorem}
\begin{proof}\uses{thm:t2_26}\end{proof}

\begin{theorem}[Exercise 2.16]
  \label{exr:s2_16}
  \lean{PrimesXNY2.PartI.S2.ex_2_16}
  \leanok
  The principal form for $D \equiv 1 \pmod 4$ is reduced of disc $D$.
\end{theorem}
\begin{proof}\uses{def:principal_form, def:reduced}\end{proof}

\begin{theorem}[Exercise 2.17(a)]
  \label{exr:s2_17_a}
  \lean{PrimesXNY2.PartI.S2.ex_2_17_a}
  \leanok
  Even values force $D \equiv 1 \pmod 8$.
\end{theorem}
\begin{proof}\uses{lem:repr_iff_equiv}\end{proof}

\begin{theorem}[Exercise 2.17(b)]
  \label{exr:s2_17_b}
  \lean{PrimesXNY2.PartI.S2.ex_2_17_b}
  \leanok
  Represented units lie in $\ker\chi$.
\end{theorem}
\begin{proof}\uses{lem:repr_iff_isSquare_general}\end{proof}

\begin{theorem}[Exercise 2.17(c)]
  \label{exr:s2_17_c}
  \lean{PrimesXNY2.PartI.S2.ex_2_17_c}
  \leanok
  $H$ is the subgroup of squares (principal form values).
\end{theorem}
\begin{proof}\uses{def:principal_form, lem:l2_24}\end{proof}

\begin{theorem}[Exercise 2.17(d)]
  \label{exr:s2_17_d}
  \notready
  The values represented by $f$ form a coset of $H$. \emph{UNVERIFIED: a faithful
  coset statement requires fixing the square subgroup and a representative;
  flagged (same delicacy as Lemma 2.24(ii)).}
\end{theorem}

\begin{theorem}[Exercise 2.18(a)]
  \label{exr:s2_18_a}
  \lean{PrimesXNY2.PartI.S2.ex_2_18_a}
  \leanok
  Some of $f(1,0),f(0,1),f(1,1)$ is prime to $p$.
\end{theorem}
\begin{proof}\uses{def:primitive}\end{proof}

\begin{theorem}[Exercise 2.18(b)]
  \label{exr:s2_18_b}
  \lean{PrimesXNY2.PartI.S2.ex_2_18_b}
  \leanok
  Proof of Lemma 2.25.
\end{theorem}
\begin{proof}\uses{lem:l2_25}\end{proof}

\begin{theorem}[Exercise 2.19]
  \label{exr:s2_19}
  \notready
  Work out the genus theory of Theorem 2.26 for $D=-15,-24,-31,-52$.
  \emph{UNVERIFIED: produces four separate congruence theorems, not one
  proposition; flagged.}
\end{theorem}

\begin{theorem}[Exercise 2.20]
  \label{exr:s2_20}
  \notready
  Corollary 2.27 for negative $D \equiv 1 \pmod 4$. \emph{UNVERIFIED: Cox does not
  give the explicit congruence for this case and I will not guess it; flagged.}
\end{theorem}

\begin{theorem}[Exercise 2.21]
  \label{exr:s2_21}
  \lean{PrimesXNY2.PartI.S2.ex_2_21}
  \leanok
  The first theorem of (2.28): $x^2+6y^2$.
\end{theorem}
\begin{proof}\uses{thm:genus_congruence}\end{proof}

\begin{theorem}[Exercise 2.22]
  \label{exr:s2_22}
  \lean{PrimesXNY2.PartI.S2.ex_2_22}
  \leanok
  The composition identity (2.31).
\end{theorem}
\begin{proof}\uses{eq:comp_2_31}\end{proof}

\begin{theorem}[Exercise 2.23(a)]
  \label{exr:s2_23_a}
  \lean{PrimesXNY2.PartI.S2.ex_2_23_a}
  \leanok
  $p \equiv 1 \pmod 8 \Rightarrow (-2/p)=1$.
\end{theorem}

\begin{theorem}[Exercise 2.23(b)]
  \label{exr:s2_23_b}
  \lean{PrimesXNY2.PartI.S2.ex_2_23_b}
  \leanok
  $p \equiv 3 \pmod 8$: $(2/p)=1$ and representation by a disc-$8$ form.
\end{theorem}
\begin{proof}\uses{cor:c2_6}\end{proof}

\begin{theorem}[Exercise 2.23(c)]
  \label{exr:s2_23_c}
  \lean{PrimesXNY2.PartI.S2.ex_2_23_c}
  \leanok
  Forms of discriminant $8$ are $\pm(x^2-2y^2)$.
\end{theorem}
\begin{proof}\uses{thm:reduction}\end{proof}

\begin{theorem}[Exercise 2.23(d)]
  \label{exr:s2_23_d}
  \lean{PrimesXNY2.PartI.S2.ex_2_23_d}
  \leanok
  $p = \pm(x^2-2y^2) \Rightarrow p \equiv \pm 1 \pmod 8$.
\end{theorem}

\begin{theorem}[Exercise 2.24]
  \label{exr:s2_24}
  \lean{PrimesXNY2.PartI.S2.ex_2_24}
  \leanok
  Legendre's theorem on $ax^2+by^2+cz^2=0$.
\end{theorem}

\begin{theorem}[Exercise 2.24(a)]
  \label{exr:s2_24_a}
  \lean{PrimesXNY2.PartI.S2.ex_2_24_a}
  \leanok
  $x^2+py^2-qz^2=0$ has a nontrivial solution.
\end{theorem}
\begin{proof}\uses{exr:s2_24}\end{proof}

\begin{theorem}[Exercise 2.24(b)]
  \label{exr:s2_24_b}
  \lean{PrimesXNY2.PartI.S2.ex_2_24_b}
  \leanok
  The mod-$4$ obstruction (contradiction proving reciprocity).
\end{theorem}

\begin{theorem}[Exercise 2.25]
  \label{exr:s2_25}
  \lean{PrimesXNY2.PartI.S2.ex_2_25}
  \leanok
  Forms are properly equivalent iff their opposites are.
\end{theorem}
\begin{proof}\uses{def:proper_equiv}\end{proof}

\begin{theorem}[Exercise 2.26]
  \label{exr:s2_26}
  \lean{PrimesXNY2.PartI.S2.ex_2_26}
  \leanok
  The four compositions lie in distinct classes.
\end{theorem}
\begin{proof}\uses{def:proper_equiv, exr:s2_6}\end{proof}

\begin{theorem}[Exercise 2.27(a)]
  \label{exr:s2_27_a}
  \lean{PrimesXNY2.PartI.S2.ex_2_27_a}
  \leanok
  A prime represented by two forms of the same disc: they are equivalent.
\end{theorem}
\begin{proof}\uses{lem:repr_iff_equiv, def:equivalent}\end{proof}

\begin{theorem}[Exercise 2.27(b)]
  \label{exr:s2_27_b}
  \lean{PrimesXNY2.PartI.S2.ex_2_27_b}
  \leanok
  A reduced form equivalent to $x^2+ny^2$ equals it.
\end{theorem}
\begin{proof}\uses{thm:reduction}\end{proof}

\section{Genus theory of Gauss and convenient numbers}

\begin{definition}[Assigned characters $\Psi$; Cox (3.16)]
  \label{def:assigned_characters}
  \lean{PrimesXNY2.Genera.Psi}
  \leanok
  \uses{def:mu}
  The homomorphism $\Psi:(\mathbb Z/D\mathbb Z)^\times \to \{\pm1\}^\mu$ assembling
  the characters $\chi_i(\cdot)=(\cdot/p_i)$, $\delta$, and $\varepsilon$.
\end{definition}

\begin{definition}[Genus map $\Phi$; Cox (3.12)]
  \label{def:genus_map}
  \lean{PrimesXNY2.Genera.genusVector}
  \leanok
  \uses{def:form_class_group, def:mu}
  The complete-character map $\Phi:C(D)\to\{\pm1\}^\mu$; its fibers are the genera.
\end{definition}

\begin{lemma}[Cox Lemma 3.13]
  \label{lem:l3_13}
  \lean{PrimesXNY2.Genera.lemma_3_13}
  \leanok
  \uses{def:genus_map, def:dirichlet_composition}
  The genus map is a homomorphism for Dirichlet composition.
\end{lemma}

\begin{theorem}[Cox Corollary 3.14(i)]
  \label{cor:c3_14_i}
  \lean{PrimesXNY2.Genera.cor_3_14_i}
  \leanok
  \uses{lem:l3_13}
  All genera of discriminant $D$ consist of the same number of classes.
\end{theorem}

\begin{theorem}[Cox Corollary 3.14(ii)]
  \label{cor:c3_14_ii}
  \lean{PrimesXNY2.Genera.cor_3_14_ii}
  \leanok
  \uses{lem:l3_13}
  The number of genera is a power of two.
\end{theorem}

\begin{lemma}[Cox Lemma 3.17]
  \label{lem:l3_17}
  \lean{PrimesXNY2.Genera.lemma_3_17}
  \leanok
  \uses{def:assigned_characters, lem:l2_24}
  $\Psi$ is surjective with kernel the values represented by the principal form, so
  $(\mathbb Z/D\mathbb Z)^\times/H \cong \{\pm1\}^\mu$.
\end{lemma}

\begin{theorem}[Number of genera; Cox Thm 3.15(i)]
  \label{thm:t3_15_i}
  \lean{PrimesXNY2.Genera.thm_3_15_i}
  \leanok
  \uses{def:genus_map, def:mu, lem:l3_17}
  There are exactly $2^{\mu-1}$ genera of forms of discriminant $D$.
\end{theorem}

\begin{theorem}[Principal genus is squares; Cox Thm 3.15(ii)]
  \label{thm:t3_15_ii}
  \lean{PrimesXNY2.Genera.thm_3_15_ii}
  \leanok
  \uses{def:genus_map, def:principal_genus, prop:p3_11}
  The principal genus is exactly the subgroup of squares $C(D)^2$.
\end{theorem}

\begin{lemma}[Cox Lemma 3.20]
  \label{lem:l3_20}
  \lean{PrimesXNY2.Genera.lemma_3_20}
  \leanok
  \uses{def:genus, def:assigned_characters}
  Two forms lie in the same genus iff they have the same complete character.
\end{lemma}

\begin{theorem}[Genus via congruences; Cox Thm 3.21]
  \label{thm:t3_21}
  \lean{PrimesXNY2.Genera.thm_3_21}
  \leanok
  \uses{def:genus}
  For primitive positive definite forms, same genus $\iff$ same values in
  $(\mathbb Z/m\mathbb Z)^\times$ for all nonzero $m$ (Cox's conditions (i)–(ii);
  the $\mathbb Z_p$/$\mathbb Q$ conditions (iii)–(vi) are deferred — see ROADMAP).
\end{theorem}

\begin{theorem}[Convenient-number characterization; Cox Thm 3.22]
  \label{thm:t3_22}
  \lean{PrimesXNY2.Genera.thm_3_22}
  \leanok
  \uses{def:genus_map, def:mu, thm:landau}
  For $n>0$: every genus of disc $-4n$ is a single class $\iff$ every reduced form
  has $b=0,a=b,a=c$ $\iff$ equivalence $=$ proper equivalence $\iff h(-4n)=2^{\mu-1}$
  (the group-isomorphism condition $C(-4n)\cong(\mathbb Z/2\mathbb Z)^k$ deferred).
\end{theorem}

\begin{definition}[Euler's convenient (idoneal) number]
  \label{def:convenient_number}
  \lean{PrimesXNY2.Genera.ConvenientNumber}
  \leanok
  $n$ is convenient if every odd $m$ coprime to $n$ that is properly represented by
  $x^2+ny^2$ with a unique solution $x,y\ge0$ is prime.
\end{definition}

\begin{proposition}[Cox Proposition 3.24]
  \label{prop:p3_24}
  \lean{PrimesXNY2.Genera.prop_3_24}
  \leanok
  \uses{def:convenient_number, thm:t3_22}
  $n$ is convenient iff every genus of disc $-4n$ consists of a single class.
\end{proposition}

\begin{lemma}[Cox Lemma 3.25]
  \label{lem:l3_25}
  \lean{PrimesXNY2.Genera.lemma_3_25}
  \leanok
  \uses{def:genus}
  The number of proper representations of odd $m$ by reduced forms of disc $-4n$ is
  $2\prod_{p\mid m}(1+(-n/p))$.
\end{lemma}

\begin{theorem}[Cox Corollary 3.26]
  \label{cor:c3_26}
  \lean{PrimesXNY2.Genera.cor_3_26}
  \leanok
  \uses{lem:l3_25, def:genus}
  An odd $m$ coprime to $4n$ properly represented by a primitive form $f$ of disc
  $-4n$ is so represented in exactly $2^{r+1}$ ways by a reduced form in the genus
  of $f$, where $r$ is the number of prime divisors of $m$.
\end{theorem}

\subsection{Exercises}

\begin{theorem}[Exercise 3.1(a)]
  \label{exr:s3_1_a}
  \lean{PrimesXNY2.PartI.S3.ex_3_1_a}
  \leanok
  \uses{def:dirichlet_composition}
  Gauss's coefficient formulas for a composition of forms.
\end{theorem}

\begin{theorem}[Exercise 3.1(b)]
  \label{exr:s3_1_b}
  \lean{PrimesXNY2.PartI.S3.ex_3_1_b}
  \leanok
  \uses{def:discriminant}
  $a^2(b'^2-4a'c') = (a_1b_2-a_2b_1)^2(B^2-4AC)$.
\end{theorem}

\begin{theorem}[Exercise 3.1(c)]
  \label{exr:s3_1_c}
  \lean{PrimesXNY2.PartI.S3.ex_3_1_c}
  \leanok
  \uses{def:dirichlet_composition}
  $a' = \pm(a_1c_2-a_2c_1)$.
\end{theorem}

\begin{theorem}[Exercise 3.2]
  \label{exr:s3_2}
  \notready
  \uses{eq:comp_2_30, eq:comp_2_31}
  The compositions (2.30) and (2.31) are not direct compositions. (Needs the
  direct-composition predicate (3.1); deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.3]
  \label{exr:s3_3}
  \notready
  \uses{lem:l3_5}
  Prove Lemma 3.5. (Proof of a spine lemma; deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.4]
  \label{exr:s3_4}
  \notready
  \uses{lem:l3_5}
  Verify the congruences (3.4) satisfy the compatibility of Lemma 3.5. (Internal to
  the composition construction; deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.5(a)]
  \label{exr:s3_5_a}
  \notready
  \uses{def:dirichlet_form, def:proper_equiv}
  $f$ is properly equivalent to $ax^2+Bxy+a'Cy^2$. (Step in showing Dirichlet
  composition is a direct composition; deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.5(b)]
  \label{exr:s3_5_b}
  \lean{PrimesXNY2.PartI.S3.ex_3_5_b}
  \leanok
  \uses{def:dirichlet_form}
  The bilinear identity exhibiting Dirichlet composition as a direct composition.
\end{theorem}
\begin{proof}\leanok\end{proof}

\begin{theorem}[Exercise 3.5(c)]
  \label{exr:s3_5_c}
  \notready
  \uses{def:dirichlet_form}
  Direct composition is preserved under proper equivalence of a factor. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.5(d)]
  \label{exr:s3_5_d}
  \notready
  \uses{def:dirichlet_form}
  Dirichlet composition is a direct composition. (Deferred; combines (a)–(c).)
\end{theorem}

\begin{theorem}[Exercise 3.5(e)]
  \label{exr:s3_5_e}
  \lean{PrimesXNY2.PartI.S3.ex_3_5_e}
  \leanok
  \uses{def:dirichlet_form, def:primitive}
  The Dirichlet composition of primitive forms is primitive.
\end{theorem}

\begin{theorem}[Exercise 3.6(a)]
  \label{exr:s3_6_a}
  \notready
  \uses{def:dirichlet_form, eq:comp_2_31}
  Dirichlet's formula matches Legendre's (2.32) when $\gcd(a,a')=1$. (Needs both
  composition formulas; deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.6(b)]
  \label{exr:s3_6_b}
  \notready
  \uses{def:dirichlet_form}
  Which of $126x^2\pm74xy+13y^2$ is the direct composition. (Needs the
  direct-composition predicate; deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.7]
  \label{exr:s3_7}
  \lean{PrimesXNY2.PartI.S3.ex_3_7}
  \leanok
  \uses{def:proper_equiv, def:principal_form}
  $acx^2+bxy+y^2$ is properly equivalent to the principal form.
\end{theorem}

\begin{theorem}[Exercise 3.8(a)]
  \label{exr:s3_8_a}
  \lean{PrimesXNY2.PartI.S3.ex_3_8_a}
  \leanok
  \uses{def:equivalent, def:opposite_form}
  The Lagrangian class is the union of the proper class and its opposite.
\end{theorem}

\begin{theorem}[Exercise 3.8(b)]
  \label{exr:s3_8_b}
  \lean{PrimesXNY2.PartI.S3.ex_3_8_b}
  \leanok
  \uses{def:equivalent, thm:t3_9_inverse}
  Equivalent conditions for the Lagrangian class to equal the proper class (Cox's
  order-$\le2$ condition deferred).
\end{theorem}

\begin{theorem}[Exercise 3.9(a)]
  \label{exr:s3_9_a}
  \notready
  \uses{def:form_class_group}
  Legendre's ``almost'' multiplication on $G/\!\sim$. (Abstract quotient structure;
  deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.9(b)]
  \label{exr:s3_9_b}
  \notready
  \uses{def:form_class_group}
  The structure of $G/\!\sim$ for $G$ cyclic of order $8$. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.9(c)]
  \label{exr:s3_9_c}
  \notready
  \uses{def:form_class_group, def:equivalent}
  $C(D)/\!\sim$ is the set of Lagrangian classes. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.10]
  \label{exr:s3_10}
  \notready
  \uses{prop:p3_11}
  Complete the proof of Proposition 3.11 in the remaining cases. (Proof of a spine
  proposition; deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.11(a)]
  \label{exr:s3_11_a}
  \notready
  \uses{lem:l3_17}
  The map (3.18) is surjective with kernel the squares. (Internal to Lemma 3.17;
  deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.11(b)]
  \label{exr:s3_11_b}
  \notready
  \uses{lem:l3_17}
  Prove Lemma 3.17 when $D=-4n$. (Proof of a spine lemma; deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.12(a)]
  \label{exr:s3_12_a}
  \notready
  \uses{def:genus_map}
  The genus map $\Phi'$ for a general nonsquare discriminant. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.12(b)]
  \label{exr:s3_12_b}
  \lean{PrimesXNY2.PartI.S3.ex_3_12_b}
  \leanok
  \uses{def:genus_map, def:mu, prop:p3_11}
  The number of genera is at most $2^{\mu-1}$.
\end{theorem}

\begin{theorem}[Exercise 3.13(a)]
  \label{exr:s3_13_a}
  \notready
  \uses{lem:l3_17}
  Lemma 3.17 holds for all nonzero discriminants. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.13(b)]
  \label{exr:s3_13_b}
  \notready
  \uses{lem:repr_iff_isSquare_general}
  $(p^*/q)=1 \Rightarrow (q/p)=1$ via genus counting. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.13(c)]
  \label{exr:s3_13_c}
  \notready
  \uses{thm:quadratic_reciprocity}
  The case $p\equiv1$ or $q\equiv1 \pmod 4$. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.13(d)]
  \label{exr:s3_13_d}
  \notready
  \uses{thm:quadratic_reciprocity}
  The case $p\equiv q\equiv3 \pmod 4$, completing quadratic reciprocity. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.13(e)]
  \label{exr:s3_13_e}
  \lean{PrimesXNY2.PartI.S3.ex_3_13_e}
  \leanok
  \uses{def:legendre_symbol}
  The second supplement $(2/p)=(-1)^{(p^2-1)/8}$.
\end{theorem}
\begin{proof}\leanok\end{proof}

\begin{theorem}[Exercise 3.14]
  \label{exr:s3_14}
  \notready
  \uses{def:genus}
  Divide the forms of discriminant $-164$ into genera. (Computational enumeration;
  deferred — see ROADMAP.)
\end{theorem}

\begin{theorem}[Exercise 3.15]
  \label{exr:s3_15}
  \notready
  \uses{thm:t3_21}
  Prove the implications $(vi)\Rightarrow(iii)\Rightarrow(ii)\Rightarrow(i)$ and
  $(vi)\Rightarrow(v)\Rightarrow(i)$ of Theorem 3.21. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.16]
  \label{exr:s3_16}
  \lean{PrimesXNY2.PartI.S3.ex_3_16}
  \leanok
  \uses{def:genus}
  $x^2+18y^2$ and $2x^2+9y^2$ share discriminant $-72$ but lie in different genera.
\end{theorem}

\begin{theorem}[Exercise 3.17(a)]
  \label{exr:s3_17_a}
  \notready
  \uses{exr:s2_1}
  A rationally equivalent form represents $d^2m$. (Step; deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.17(b)]
  \label{exr:s3_17_b}
  \notready
  \uses{lem:repr_iff_equiv}
  Rationally equivalent forms of a field discriminant lie in the same genus.
  (Needs $\mathbb Q$-equivalence; deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.18(a)]
  \label{exr:s3_18_a}
  \notready
  \uses{thm:t3_21}
  A principal-genus form represents an odd square $a^2$ coprime to $n$. (Step;
  deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.18(b)]
  \label{exr:s3_18_b}
  \notready
  \uses{def:dirichlet_form}
  The associated form $ax^2+2bxy+acy^2$ is primitive of disc $-4n$. (Step; deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.18(c)]
  \label{exr:s3_18_c}
  \notready
  \uses{thm:t3_15_ii, def:dirichlet_form}
  A principal-genus form is a Dirichlet square. (Deferred; re-proves Thm 3.15(ii).)
\end{theorem}

\begin{theorem}[Exercise 3.19]
  \label{exr:s3_19}
  \notready
  \uses{thm:t3_22, def:genus_map}
  Equivalent conditions for one class per genus, incl. $C(D)\cong\{\pm1\}^{\mu-1}$.
  (Group-isomorphism condition; deferred — subsumes Thm 3.22.)
\end{theorem}

\begin{theorem}[Exercise 3.20(a)]
  \label{exr:s3_20_a}
  \lean{PrimesXNY2.PartI.S3.ex_3_20_a}
  \leanok
  \uses{lem:l3_25}
  The number of solutions of $x^2\equiv-n \pmod m$ is $\prod_{p\mid m}(1+(-n/p))$.
\end{theorem}

\begin{theorem}[Exercise 3.20(b)]
  \label{exr:s3_20_b}
  \notready
  \uses{lem:l3_25}
  Forms $mx^2+2bxy+cy^2$ correspond to solutions of $x^2\equiv-n \pmod m$.
  (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.20(c)]
  \label{exr:s3_20_c}
  \notready
  \uses{lem:l3_25}
  Construction of the form $g_{u,v}$ from a proper representation. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.20(d)]
  \label{exr:s3_20_d}
  \notready
  \uses{lem:l3_25}
  The map from representations to forms is onto. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.20(e)]
  \label{exr:s3_20_e}
  \notready
  \uses{lem:l3_25, thm:reduction}
  Fibres of the map use uniqueness of reduction. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.20(f)]
  \label{exr:s3_20_f}
  \notready
  \uses{lem:l3_25}
  The map is exactly two-to-one, proving Lemma 3.25. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.21(a)]
  \label{exr:s3_21_a}
  \notready
  \uses{lem:l3_25}
  $m=x^2+2y^2$ and $m^3=x^2+2y^2$ have the same number of proper solutions.
  (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.21(b)]
  \label{exr:s3_21_b}
  \lean{PrimesXNY2.PartI.S3.ex_3_21_b}
  \leanok
  $m=a^2+2b^2 \Rightarrow m^3=(a^3-6ab^2)^2+2(3a^2b-2b^3)^2$.
\end{theorem}
\begin{proof}\leanok\end{proof}

\begin{theorem}[Exercise 3.21(c)]
  \label{exr:s3_21_c}
  \lean{PrimesXNY2.PartI.S3.ex_3_21_c}
  \leanok
  The cubing map on $\mathbb Z[\sqrt{-2}]$ is injective.
\end{theorem}

\begin{theorem}[Exercise 3.21(d)]
  \label{exr:s3_21_d}
  \notready
  \uses{lem:l3_25, exr:s3_21_b}
  All proper representations of $m^3$ arise from those of $m$. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.22]
  \label{exr:s3_22}
  \lean{PrimesXNY2.PartI.S3.ex_3_22}
  \leanok
  \uses{exr:s3_21_b}
  Fermat: the only integer solutions of $x^3=y^2+2$ are $(3,\pm5)$.
\end{theorem}

\begin{theorem}[Exercise 3.23]
  \label{exr:s3_23}
  \lean{PrimesXNY2.PartI.S3.ex_3_23}
  \leanok
  \uses{lem:l3_25, thm:genus_congruence}
  An odd prime $p=x^2+ny^2$ ($n>1$) has a unique representation with $x,y\ge0$.
\end{theorem}

\begin{theorem}[Exercise 3.24(a)]
  \label{exr:s3_24_a}
  \notready
  \uses{def:legendre_symbol}
  Reciprocity for the extended Jacobi symbol (odd, coprime, signed). (Needs the
  Kronecker symbol; deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.24(b)]
  \label{exr:s3_24_b}
  \notready
  \uses{def:legendre_symbol}
  Gauss's count-of-nonresidue version of (a). (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.24(c)]
  \label{exr:s3_24_c}
  \notready
  \uses{def:legendre_symbol}
  Supplementary laws for the extended symbol. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.25(a)]
  \label{exr:s3_25_a}
  \notready
  \uses{thm:t3_15_i}
  $C(-4p)\cong(\mathbb Z/2^a\mathbb Z)\times G$ with $G$ of odd order, so $2\mid h(-4p)$.
  (Group structure; deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.25(b)]
  \label{exr:s3_25_b}
  \notready
  \uses{def:genus}
  $2x^2+2xy+\tfrac{p+1}{2}y^2$ lies in the principal genus. (Computational; deferred.)
\end{theorem}

\begin{theorem}[Exercise 3.25(c)]
  \label{exr:s3_25_c}
  \notready
  \uses{thm:t3_15_ii}
  $C(-4p)$ has an element of order $4$, so $4\mid h(-4p)$. (Deferred.)
\end{theorem}

\section{Cubic and biquadratic reciprocity}

\subsection{Eisenstein integers and cubic reciprocity}

\begin{definition}[Eisenstein integers]
  \label{def:eisenstein_int}
  \lean{PrimesXNY2.CubicReciprocity.EisensteinInt}
  \leanok
  The ring $\mathbb Z[\omega]$, $\omega = e^{2\pi i/3}$, as a lightweight structure
  $\{a + b\omega\}$ (Mathlib has no Eisenstein integers; see ROADMAP).
\end{definition}

\begin{definition}[Eisenstein norm]
  \label{def:eisenstein_norm}
  \lean{PrimesXNY2.CubicReciprocity.EisensteinInt.norm}
  \leanok
  \uses{def:eisenstein_int}
  $N(a + b\omega) = a^2 - ab + b^2$.
\end{definition}

\begin{lemma}[Norm is multiplicative]
  \label{lem:eisenstein_norm_mul}
  \lean{PrimesXNY2.CubicReciprocity.EisensteinInt.norm_mul}
  \leanok
  \uses{def:eisenstein_norm}
  $N(\alpha\beta) = N(\alpha)N(\beta)$.
\end{lemma}
\begin{proof}\leanok\end{proof}

\begin{proposition}[Cox Proposition 4.3]
  \label{prop:p4_3}
  \lean{PrimesXNY2.CubicReciprocity.EisensteinInt.prop_4_3}
  \leanok
  \uses{def:eisenstein_norm}
  $\mathbb Z[\omega]$ is a Euclidean ring.
\end{proposition}

\begin{theorem}[Cox Corollary 4.4]
  \label{cor:c4_4}
  \lean{PrimesXNY2.CubicReciprocity.EisensteinInt.cor_4_4}
  \leanok
  \uses{prop:p4_3}
  $\mathbb Z[\omega]$ is a PID and a UFD. Formalized as the hallmark consequence
  irreducible $\iff$ prime; the full PID/UFD typeclass is deferred.
\end{theorem}

\begin{lemma}[Cox Lemma 4.5(i)]
  \label{lem:l4_5_i}
  \lean{PrimesXNY2.CubicReciprocity.EisensteinInt.lemma_4_5_i}
  \leanok
  \uses{def:eisenstein_norm}
  $x$ is a unit iff $N(x) = 1$.
\end{lemma}

\begin{lemma}[Cox Lemma 4.5(ii)]
  \label{lem:l4_5_ii}
  \lean{PrimesXNY2.CubicReciprocity.EisensteinInt.lemma_4_5_ii}
  \leanok
  \uses{lem:l4_5_i}
  The units of $\mathbb Z[\omega]$ are $\{\pm1, \pm\omega, \pm\omega^2\}$.
\end{lemma}

\begin{lemma}[Cox Lemma 4.6]
  \label{lem:l4_6}
  \lean{PrimesXNY2.CubicReciprocity.EisensteinInt.lemma_4_6}
  \leanok
  \uses{def:eisenstein_norm}
  If $N(\alpha)$ is a rational prime then $\alpha$ is prime in $\mathbb Z[\omega]$.
\end{lemma}

\begin{proposition}[Cox Proposition 4.7(i); ramified]
  \label{prop:p4_7_ramified}
  \lean{PrimesXNY2.CubicReciprocity.EisensteinInt.prop_4_7_ramified}
  \leanok
  \uses{lem:l4_6}
  $1-\omega$ is prime and $3 = -\omega^2(1-\omega)^2$.
\end{proposition}

\begin{proposition}[Cox Proposition 4.7(ii); split]
  \label{prop:p4_7_split}
  \lean{PrimesXNY2.CubicReciprocity.EisensteinInt.prop_4_7_split}
  \leanok
  \uses{lem:l4_6}
  For $p \equiv 1 \pmod 3$, $p = \pi\bar\pi$ with $\pi,\bar\pi$ nonassociate primes.
\end{proposition}

\begin{proposition}[Cox Proposition 4.7(iii); inert]
  \label{prop:p4_7_inert}
  \lean{PrimesXNY2.CubicReciprocity.EisensteinInt.prop_4_7_inert}
  \leanok
  \uses{lem:l4_6}
  For $p \equiv 2 \pmod 3$, $p$ remains prime in $\mathbb Z[\omega]$.
\end{proposition}

\begin{lemma}[Cox Lemma 4.8]
  \label{lem:l4_8}
  \lean{PrimesXNY2.CubicReciprocity.EisensteinInt.lemma_4_8}
  \leanok
  \uses{def:eisenstein_norm}
  For a prime $\pi$ over $p$, $N(\pi) = p$ or $p^2$. Formalized as this norm
  dichotomy; the residue-field cardinality $|\mathbb Z[\omega]/\pi| = N(\pi)$ is
  deferred.
\end{lemma}

\begin{theorem}[Cox Corollary 4.9; Eisenstein Fermat]
  \label{cor:c4_9}
  \lean{PrimesXNY2.CubicReciprocity.EisensteinInt.cor_4_9}
  \leanok
  \uses{lem:l4_8}
  If $\pi \nmid \alpha$ then $\alpha^{N(\pi)-1} \equiv 1 \pmod \pi$.
\end{theorem}

\begin{definition}[Cubic residue character; Cox (4.10)]
  \label{def:cubic_char}
  \lean{PrimesXNY2.CubicReciprocity.EisensteinInt.cubicChar}
  \leanok
  \uses{cor:c4_9}
  $(\alpha/\pi)_3 \in \{1,\omega,\omega^2\}$ with $\alpha^{(N(\pi)-1)/3} \equiv
  (\alpha/\pi)_3 \pmod \pi$.
\end{definition}

\begin{lemma}[Cubic character: defining property]
  \label{lem:cubic_char_spec}
  \lean{PrimesXNY2.CubicReciprocity.EisensteinInt.cubicChar_spec}
  \leanok
  \uses{def:cubic_char}
  The defining congruence and the value in $\{1,\omega,\omega^2\}$.
\end{lemma}

\begin{lemma}[Cubic character is multiplicative]
  \label{lem:cubic_char_mul}
  \lean{PrimesXNY2.CubicReciprocity.EisensteinInt.cubicChar_mul}
  \leanok
  \uses{def:cubic_char}
  $(\alpha\beta/\pi)_3 = (\alpha/\pi)_3(\beta/\pi)_3$.
\end{lemma}

\begin{lemma}[Cubic residue criterion; Cox (4.11)]
  \label{lem:cubic_residue_iff}
  \lean{PrimesXNY2.CubicReciprocity.EisensteinInt.cubicChar_eq_one_iff}
  \leanok
  \uses{def:cubic_char}
  $(\alpha/\pi)_3 = 1$ iff $x^3 \equiv \alpha \pmod \pi$ is solvable.
\end{lemma}

\begin{definition}[Primary prime; Eisenstein]
  \label{def:primary_eisenstein}
  \lean{PrimesXNY2.CubicReciprocity.EisensteinInt.IsPrimary}
  \leanok
  \uses{def:eisenstein_int}
  $\pi \equiv \pm 1 \pmod 3$ (Cox's normalization).
\end{definition}

\begin{theorem}[Cubic Reciprocity; Cox Thm 4.12]
  \label{thm:cubic_reciprocity}
  \lean{PrimesXNY2.CubicReciprocity.EisensteinInt.thm_4_12}
  \leanok
  \notready
  \uses{def:cubic_char, def:primary_eisenstein}
  For primary primes $\pi,\theta$ of unequal norm, $(\pi/\theta)_3 = (\theta/\pi)_3$.
  (Deep; GAP, never an axiom.)
\end{theorem}

\begin{lemma}[Cubic supplementary laws; Cox (4.13)]
  \label{lem:cubic_supplementary}
  \lean{PrimesXNY2.CubicReciprocity.EisensteinInt.supplementary_4_13}
  \leanok
  \notready
  \uses{def:cubic_char, def:primary_eisenstein}
  $(\omega/\pi)_3 = \omega^{m+n}$, $((1-\omega)/\pi)_3 = \omega^{2m}$ for
  $\pi = -1 + 3m + 3n\omega$. (Deep; GAP.)
\end{lemma}

\begin{lemma}[Cubic residues of integers; Cox (4.14)]
  \label{lem:cubic_int_residue}
  \lean{PrimesXNY2.CubicReciprocity.EisensteinInt.eq_4_14}
  \leanok
  \uses{def:cubic_char}
  $x^3 \equiv a \pmod p$ is solvable in $\mathbb Z$ iff $(a/\pi)_3 = 1$.
\end{lemma}

\begin{theorem}[$p = x^2 + 27y^2$; Cox Thm 4.15]
  \label{thm:x2_27y2}
  \lean{PrimesXNY2.CubicReciprocity.EisensteinInt.thm_4_15}
  \leanok
  \notready
  \uses{thm:cubic_reciprocity, lem:cubic_int_residue}
  $p = x^2 + 27y^2$ iff $p \equiv 1 \pmod 3$ and $2$ is a cubic residue mod $p$.
  (Deep; GAP, never an axiom.)
\end{theorem}

\subsection{Gaussian integers and biquadratic reciprocity}

\begin{definition}[Primary prime; Gaussian]
  \label{def:gaussian_primary}
  \lean{PrimesXNY2.BiquadraticReciprocity.IsPrimaryG}
  \leanok
  $\pi \equiv 1 \pmod{2 + 2i}$ in $\mathbb Z[i]$.
\end{definition}

\begin{proposition}[Cox Proposition 4.18(i); ramified]
  \label{prop:p4_18_ramified}
  \lean{PrimesXNY2.BiquadraticReciprocity.prop_4_18_ramified}
  \leanok
  $1+i$ is prime and $2 = i^3(1+i)^2$.
\end{proposition}

\begin{proposition}[Cox Proposition 4.18(ii); split]
  \label{prop:p4_18_split}
  \lean{PrimesXNY2.BiquadraticReciprocity.prop_4_18_split}
  \leanok
  For $p \equiv 1 \pmod 4$, $p = \pi\bar\pi$ with $\pi,\bar\pi$ nonassociate primes.
\end{proposition}

\begin{proposition}[Cox Proposition 4.18(iii); inert]
  \label{prop:p4_18_inert}
  \lean{PrimesXNY2.BiquadraticReciprocity.prop_4_18_inert}
  \leanok
  For $p \equiv 3 \pmod 4$, $p$ remains prime in $\mathbb Z[i]$.
\end{proposition}

\begin{lemma}[Cox (4.19); Gaussian Fermat]
  \label{lem:gaussian_fermat}
  \lean{PrimesXNY2.BiquadraticReciprocity.eq_4_19}
  \leanok
  If $\pi \nmid \alpha$ then $\alpha^{N(\pi)-1} \equiv 1 \pmod \pi$.
\end{lemma}

\begin{definition}[Quartic residue character; Cox (4.20)]
  \label{def:quartic_char}
  \lean{PrimesXNY2.BiquadraticReciprocity.quarticChar}
  \leanok
  \uses{lem:gaussian_fermat}
  $(\alpha/\pi)_4 \in \{1,i,-1,-i\}$ with $\alpha^{(N(\pi)-1)/4} \equiv
  (\alpha/\pi)_4 \pmod \pi$.
\end{definition}

\begin{lemma}[Quartic character: defining property]
  \label{lem:quartic_char_spec}
  \lean{PrimesXNY2.BiquadraticReciprocity.quarticChar_spec}
  \leanok
  \uses{def:quartic_char}
  The defining congruence and the value in $\{1,i,-1,-i\}$.
\end{lemma}

\begin{lemma}[Quartic character is multiplicative]
  \label{lem:quartic_char_mul}
  \lean{PrimesXNY2.BiquadraticReciprocity.quarticChar_mul}
  \leanok
  \uses{def:quartic_char}
  $(\alpha\beta/\pi)_4 = (\alpha/\pi)_4(\beta/\pi)_4$.
\end{lemma}

\begin{lemma}[Quartic residue criterion]
  \label{lem:quartic_residue_iff}
  \lean{PrimesXNY2.BiquadraticReciprocity.quarticChar_eq_one_iff}
  \leanok
  \uses{def:quartic_char}
  $(\alpha/\pi)_4 = 1$ iff $x^4 \equiv \alpha \pmod \pi$ is solvable.
\end{lemma}

\begin{theorem}[Biquadratic Reciprocity; Cox Thm 4.21]
  \label{thm:biquadratic_reciprocity}
  \lean{PrimesXNY2.BiquadraticReciprocity.thm_4_21}
  \leanok
  \notready
  \uses{def:quartic_char, def:gaussian_primary}
  For distinct primary primes, $(\pi/\theta)_4 = (\theta/\pi)_4
  (-1)^{(N(\pi)-1)(N(\theta)-1)/16}$. (Deep; GAP, never an axiom.)
\end{theorem}

\begin{lemma}[Biquadratic supplementary laws; Cox (4.22)]
  \label{lem:biquadratic_supplementary}
  \lean{PrimesXNY2.BiquadraticReciprocity.supplementary_4_22}
  \leanok
  \notready
  \uses{def:quartic_char, def:gaussian_primary}
  $(i/\pi)_4 = i^{-(a-1)/2}$, $((1+i)/\pi)_4 = i^{(a-b-1-b^2)/4}$. (Deep; GAP.)
\end{lemma}

\begin{theorem}[Quartic character of $2$; Cox Thm 4.23(i)]
  \label{thm:quartic_char_two}
  \lean{PrimesXNY2.BiquadraticReciprocity.thm_4_23_i}
  \leanok
  \notready
  \uses{lem:biquadratic_supplementary}
  For primary $\pi = a+bi$, $(2/\pi)_4 = i^{ab/2}$. (Deep; GAP.)
\end{theorem}

\begin{theorem}[$p = x^2 + 64y^2$; Cox Thm 4.23(ii)]
  \label{thm:x2_64y2}
  \lean{PrimesXNY2.BiquadraticReciprocity.thm_4_23_ii}
  \leanok
  \notready
  \uses{thm:biquadratic_reciprocity, thm:quartic_char_two}
  $p = x^2 + 64y^2$ iff $p \equiv 1 \pmod 4$ and $2$ is a biquadratic residue mod
  $p$. (Deep; GAP, never an axiom.)
\end{theorem}

\subsection{Exercises}

\begin{theorem}[Exercise 4.1]
  \label{exr:s4_1}
  \notready
  \uses{def:eisenstein_int}
  $\mathbb Z[\omega]$ and $\mathbb Z[i]$ are subrings of $\mathbb C$ closed under
  conjugation. (Needs the $\mathbb C$-embedding; deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.2(i)]
  \label{exr:s4_2_a}
  \lean{PrimesXNY2.PartI.S4.ex_4_2_a}
  \leanok
  \uses{def:eisenstein_norm}
  $N(a+b\omega) = a^2 - ab + b^2$.
\end{theorem}
\begin{proof}\leanok\end{proof}

\begin{theorem}[Exercise 4.2(ii)]
  \label{exr:s4_2_b}
  \lean{PrimesXNY2.PartI.S4.ex_4_2_b}
  \leanok
  \uses{lem:eisenstein_norm_mul}
  The norm is multiplicative.
\end{theorem}
\begin{proof}\leanok\end{proof}

\begin{theorem}[Exercise 4.3]
  \label{exr:s4_3}
  \notready
  The ideal $(x,y)$ shows $\mathbb C[x,y]$ is a UFD but not a PID. (Needs
  multivariate polynomial ideals; deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.4]
  \label{exr:s4_4}
  \lean{PrimesXNY2.PartI.S4.ex_4_4}
  \leanok
  In a PID, irreducible $\iff$ prime $\iff$ $(\alpha)$ prime ideal $\iff$
  $(\alpha)$ maximal.
\end{theorem}
\begin{proof}\leanok\end{proof}

\begin{theorem}[Exercise 4.5]
  \label{exr:s4_5}
  \notready
  \uses{lem:l4_5_i, lem:l4_5_ii}
  Prove Lemma 4.5. (Proof of a spine lemma; deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.6(a)]
  \label{exr:s4_6_a}
  \lean{PrimesXNY2.PartI.S4.ex_4_6_a}
  \leanok
  The units of $\mathbb Z[\sqrt{-3}]$ are $\pm1$.
\end{theorem}

\begin{theorem}[Exercise 4.6(b)]
  \label{exr:s4_6_b}
  \lean{PrimesXNY2.PartI.S4.ex_4_6_b}
  \leanok
  $2$ is irreducible but not prime in $\mathbb Z[\sqrt{-3}]$ (not a UFD).
\end{theorem}

\begin{theorem}[Exercise 4.6(c)]
  \label{exr:s4_6_c}
  \lean{PrimesXNY2.PartI.S4.ex_4_6_c}
  \leanok
  The ideal $(2, 1+\sqrt{-3})$ is not principal (not a PID).
\end{theorem}

\begin{theorem}[Exercise 4.7(a)]
  \label{exr:s4_7_a}
  \notready
  \uses{prop:p4_7_split}
  Prove the split case of Proposition 4.7. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.7(b)]
  \label{exr:s4_7_b}
  \notready
  \uses{prop:p4_7_inert}
  Prove the inert case of Proposition 4.7. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.8]
  \label{exr:s4_8}
  \notready
  \uses{lem:l4_8}
  Complete the proof of Lemma 4.8. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.9(a)]
  \label{exr:s4_9_a}
  \lean{PrimesXNY2.PartI.S4.ex_4_9_a}
  \leanok
  \uses{def:eisenstein_norm}
  $3 \mid N(\pi) - 1$ for $\pi$ not associate to $1-\omega$.
\end{theorem}

\begin{theorem}[Exercise 4.9(b)]
  \label{exr:s4_9_b}
  \notready
  \uses{def:cubic_char}
  $1,\omega,\omega^2$ are distinct modulo $\pi$. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.10(a)]
  \label{exr:s4_10_a}
  \lean{PrimesXNY2.PartI.S4.ex_4_10_a}
  \leanok
  \uses{def:cubic_char}
  The cubic character is multiplicative.
\end{theorem}

\begin{theorem}[Exercise 4.10(b)]
  \label{exr:s4_10_b}
  \lean{PrimesXNY2.PartI.S4.ex_4_10_b}
  \leanok
  \uses{def:cubic_char}
  The cubic character depends only on the residue mod $\pi$.
\end{theorem}

\begin{theorem}[Exercise 4.11]
  \label{exr:s4_11}
  \notready
  \uses{lem:cubic_residue_iff}
  Verify (4.11) when $(\mathbb Z[\omega]/\pi)^\times$ is cyclic. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.12]
  \label{exr:s4_12}
  \notready
  \uses{def:primary_eisenstein}
  Exactly two of the six associates of $\pi$ are primary. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.13]
  \label{exr:s4_13}
  \notready
  \uses{lem:cubic_supplementary}
  Prove the first supplementary law (4.13). (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.14]
  \label{exr:s4_14}
  \lean{PrimesXNY2.PartI.S4.ex_4_14}
  \leanok
  For $p \equiv 2 \pmod 3$, every $a$ is a cube mod $p$.
\end{theorem}

\begin{theorem}[Exercise 4.15(a)]
  \label{exr:s4_15_a}
  \notready
  \uses{thm:x2_27y2}
  $4p = a^2 + 27b^2$ gives a primary prime $\pi$ with $p = \pi\bar\pi$. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.15(b)]
  \label{exr:s4_15_b}
  \notready
  \uses{lem:cubic_supplementary}
  Rewrite the supplementary laws (4.13) in terms of $a$. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.15(c)]
  \label{exr:s4_15_c}
  \notready
  \uses{lem:cubic_supplementary}
  $(3/\pi)_3 = \omega^{2b}$. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.15(d)]
  \label{exr:s4_15_d}
  \lean{PrimesXNY2.PartI.S4.ex_4_15_d}
  \leanok
  \notready
  \uses{thm:cubic_reciprocity, lem:cubic_int_residue}
  $4p = x^2 + 243y^2$ iff $p \equiv 1 \pmod 3$ and $3$ is a cubic residue mod $p$.
  (Deep; GAP.)
\end{theorem}

\begin{theorem}[Exercise 4.16(a)]
  \label{exr:s4_16_a}
  \notready
  Prove $\mathbb Z[i]$ is Euclidean. (Deferred; Mathlib provides this.)
\end{theorem}

\begin{theorem}[Exercise 4.16(b)]
  \label{exr:s4_16_b}
  \notready
  Prove the $\mathbb Z[i]$ analogs of Lemmas 4.5 and 4.6. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.16(c)]
  \label{exr:s4_16_c}
  \notready
  \uses{prop:p4_18_split}
  Prove Proposition 4.18. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.16(d)]
  \label{exr:s4_16_d}
  \notready
  Prove the $\mathbb Z[i]$ analog of Lemma 4.8. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.16(e)]
  \label{exr:s4_16_e}
  \notready
  \uses{lem:gaussian_fermat}
  Prove (4.19). (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.17]
  \label{exr:s4_17}
  \lean{PrimesXNY2.PartI.S4.ex_4_17}
  \leanok
  $4 \mid N(\pi) - 1$ for $\pi$ not associate to $1+i$.
\end{theorem}

\begin{theorem}[Exercise 4.18(a)]
  \label{exr:s4_18_a}
  \notready
  \uses{def:quartic_char}
  The quartic character is well-defined (4.20). (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.18(b)]
  \label{exr:s4_18_b}
  \lean{PrimesXNY2.PartI.S4.ex_4_18_b}
  \leanok
  \uses{def:quartic_char}
  The quartic character is multiplicative.
\end{theorem}

\begin{theorem}[Exercise 4.18(c)]
  \label{exr:s4_18_c}
  \notready
  \uses{lem:quartic_residue_iff}
  $(\alpha/\pi)_4 = 1$ iff $x^4 \equiv \alpha \pmod \pi$ solvable. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.19(a)]
  \label{exr:s4_19_a}
  \notready
  \uses{def:quartic_char}
  $(a/\pi)_4 = \pm1$ and $= \pm1$ iff $(a/p) = 1$. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.19(b)]
  \label{exr:s4_19_b}
  \notready
  \uses{def:quartic_char}
  Verify the congruence (4.20). (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.20(a)]
  \label{exr:s4_20_a}
  \notready
  \uses{def:quartic_char}
  For $p \equiv 3 \pmod 4$, $a$ is a fourth power mod $p$ in $\mathbb Z[i]$.
  (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.20(b)]
  \label{exr:s4_20_b}
  \notready
  $a$ is a biquadratic residue mod $p$ iff $(a/p) = 1$. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.21]
  \label{exr:s4_21}
  \notready
  \uses{def:gaussian_primary}
  $\pi$ has a unique primary associate. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.22]
  \label{exr:s4_22}
  \notready
  \uses{lem:biquadratic_supplementary}
  Prove the first supplementary law (4.22). (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.23]
  \label{exr:s4_23}
  \notready
  \uses{thm:quartic_char_two}
  Prove Theorem 4.23(i) from (4.22). (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.24(a)]
  \label{exr:s4_24_a}
  \notready
  \uses{thm:quadratic_reciprocity}
  $(a/p) = 1$ via Jacobi reciprocity. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.24(b)]
  \label{exr:s4_24_b}
  \lean{PrimesXNY2.PartI.S4.ex_4_24_b}
  \leanok
  $2(a^2+b^2) = (a+b)^2 + (a-b)^2$.
\end{theorem}
\begin{proof}\leanok\end{proof}

\begin{theorem}[Exercise 4.24(c)]
  \label{exr:s4_24_c}
  \notready
  \uses{lem:biquadratic_supplementary}
  $((a+b)/p) = (i/\pi)_4 i^{ab/2}$. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.24(d)]
  \label{exr:s4_24_d}
  \notready
  \uses{def:quartic_char}
  $(a+b)^{(p-1)/2} \equiv (2ab)^{(p-1)/4}$ and $((a+b)/\pi)_4 = (2ab/\pi)_4$.
  (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.24(e)]
  \label{exr:s4_24_e}
  \notready
  \uses{def:quartic_char}
  $(2ab/\pi)_4 = (2i/\pi)_4$. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.24(f)]
  \label{exr:s4_24_f}
  \notready
  \uses{thm:quartic_char_two}
  Combine to obtain $(2/\pi)_4 = i^{ab/2}$. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.25(a)]
  \label{exr:s4_25_a}
  \notready
  \uses{def:gaussian_primary}
  A primary $\pi$ is $\equiv 1$ or $3 + 2i \pmod 4$. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.25(b)]
  \label{exr:s4_25_b}
  \notready
  \uses{thm:biquadratic_reciprocity}
  Gauss's form of biquadratic reciprocity. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.26]
  \label{exr:s4_26}
  \notready
  \uses{def:quartic_char}
  If $2$ is a biquadratic residue mod $p$ then $p \equiv \pm1 \pmod 8$. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.27(a)]
  \label{exr:s4_27_a}
  \notready
  \uses{def:quartic_char}
  $(-1/\pi)_4 = 1$ when $p \equiv 1 \pmod 4$. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.27(b)]
  \label{exr:s4_27_b}
  \notready
  \uses{thm:quadratic_reciprocity}
  $(a/p) = (-1)^{(a^2-1)/8}$. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.27(c)]
  \label{exr:s4_27_c}
  \notready
  \uses{thm:quadratic_reciprocity}
  $(b/p) = 1$. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.27(d)]
  \label{exr:s4_27_d}
  \notready
  \uses{thm:quartic_char_two}
  $(2/\pi)_4 = (-1)^{(a^2-1)/8}$, Gauss's claim. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.28]
  \label{exr:s4_28}
  \notready
  Gaussian period multiplication $(f,\lambda)(f,\mu) = \sum_j (f,\lambda+\mu_j)$.
  (Needs Gaussian periods; deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.29(a)]
  \label{exr:s4_29_a}
  \notready
  \uses{exr:s4_28}
  The count $N = 9(00) + 6$ of solutions of $x^3 - y^3 \equiv 1$. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.29(b)]
  \label{exr:s4_29_b}
  \notready
  \uses{exr:s4_28}
  Period products via Exercise 4.28. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.29(c)]
  \label{exr:s4_29_c}
  \notready
  The nine $(ij)$ reduce to three quantities $\alpha,\beta,\gamma$. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.29(d)]
  \label{exr:s4_29_d}
  \notready
  Definitions of $\alpha,\beta,\gamma$. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.29(e)]
  \label{exr:s4_29_e}
  \notready
  $\alpha^2+\beta^2+\gamma^2-\alpha = \alpha\beta+\beta\gamma+\gamma\alpha$.
  (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.29(f)]
  \label{exr:s4_29_f}
  \notready
  $(6\alpha-3\beta-3\gamma-2)^2 + 27(\beta-\gamma)^2 = 12(\alpha+\beta+\gamma)-4$.
  (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.29(g)]
  \label{exr:s4_29_g}
  \notready
  $4p = a^2 + 27b^2$ with $a = 6\alpha-3\beta-3\gamma-2$. (Deferred.)
\end{theorem}

\begin{theorem}[Exercise 4.29(h)]
  \label{exr:s4_29_h}
  \notready
  $a = N - p + 2$, proving (4.24). (Deferred.)
\end{theorem}

\chapter{Part II — Class Field Theory}

This part states the structural bridge between forms and ideals, then the deep
class field theory inputs (Artin reciprocity, Čebotarev) that current Mathlib does
not yet provide; those are stated to be cited or assumed. See ROADMAP.md.

\section{Orders in imaginary quadratic fields}

\begin{definition}[Order]
  \label{def:quad_order}
  \lean{PrimesXNY2.Order.QuadOrder}
  \leanok
  An order $\mathcal O = \mathbb Z + f\,\mathcal O_K$ of conductor $f$ in an
  imaginary quadratic field $K$.
\end{definition}

\begin{definition}[Discriminant of an order]
  \label{def:order_discriminant}
  \lean{PrimesXNY2.Order.QuadOrder.discr}
  \leanok
  \uses{def:quad_order}
  The discriminant $D = f^2 d_K$ of the order $\mathcal O$.
\end{definition}

\begin{definition}[Ideal class group of an order]
  \label{def:ideal_class_group_order}
  \lean{PrimesXNY2.Order.QuadOrder.idealClassGroup}
  \leanok
  \uses{def:quad_order}
  $C(\mathcal O)$: proper fractional $\mathcal O$-ideals modulo principal ideals.
\end{definition}

\begin{theorem}[$C(\mathcal O)$ is a finite abelian group; Cox Prop 7.19]
  \label{thm:ideal_class_group_finite}
  \lean{PrimesXNY2.Order.QuadOrder.idealClassGroup_isCommGroup}
  \leanok
  \uses{def:ideal_class_group_order}
  The ideal class group of an order is a finite abelian group.
\end{theorem}

\section{The bridge: forms and ideals}

\begin{theorem}[Bridge; Cox Thm 7.7]
  \label{thm:form_ideal_bridge}
  \lean{PrimesXNY2.Bridge.formClassGroup_equiv_idealClassGroup}
  \leanok
  For the order $\mathcal O$ of discriminant $D < 0$, the form class group $C(D)$ is
  isomorphic, as a group, to the ideal class group $C(\mathcal O)$.
\end{theorem}
\begin{proof}
  \uses{def:form_class_group, def:ideal_class_group_order, def:order_discriminant}
\end{proof}

\begin{theorem}[Bridge respects composition]
  \label{thm:bridge_composition}
  \lean{PrimesXNY2.Bridge.bridge_respects_composition}
  \leanok
  The bridge intertwines Dirichlet composition with ideal multiplication.
\end{theorem}
\begin{proof}
  \uses{thm:form_ideal_bridge, def:dirichlet_composition}
\end{proof}

\section{Class fields, Artin reciprocity, Čebotarev (deep)}

\begin{definition}[Hilbert class field]
  \label{def:hilbert_class_field}
  \lean{PrimesXNY2.RingClassField.hilbertClassField}
  \leanok
  \uses{def:quad_order}
  The maximal unramified abelian extension $H$ of $K$.
\end{definition}

\begin{definition}[Ring class field]
  \label{def:ring_class_field}
  \lean{PrimesXNY2.RingClassField.ringClassField}
  \leanok
  \uses{def:quad_order}
  The abelian extension $L/K$ with $\mathrm{Gal}(L/K) \cong C(\mathcal O)$.
\end{definition}

\begin{theorem}[Artin reciprocity; Cox Thm 8.2 — deep]
  \label{thm:artin_reciprocity}
  \lean{PrimesXNY2.RingClassField.artinReciprocity}
  \leanok
  The Artin map induces an isomorphism $C(\mathcal O) \cong \mathrm{Gal}(L/K)$.
  \emph{Deep: cite or assume.}
\end{theorem}
\begin{proof}
  \uses{def:ring_class_field, def:ideal_class_group_order}
\end{proof}

\begin{definition}[Complete splitting]
  \label{def:splits_completely}
  \lean{PrimesXNY2.RingClassField.SplitsCompletely}
  \leanok
  \uses{def:ring_class_field}
  A prime $p$ splits completely in the ring class field of $\mathcal O$.
\end{definition}

\begin{theorem}[Čebotarev density; deep]
  \label{thm:chebotarev}
  \lean{PrimesXNY2.RingClassField.chebotarev_splitting}
  \leanok
  The primes splitting completely in the ring class field are infinite (density
  $1/|C(\mathcal O)|$). \emph{Deep: cite or assume.}
\end{theorem}
\begin{proof}
  \uses{def:splits_completely}
\end{proof}

\section{The Main Theorem}

\begin{definition}[Class polynomial]
  \label{def:class_polynomial}
  \lean{PrimesXNY2.MainTheorem.classPolynomial}
  \leanok
  \uses{def:ring_class_field}
  The integer polynomial $f_n(x)$ whose roots generate the ring class field of
  $\mathbb Z[\sqrt{-n}]$.
\end{definition}

\begin{theorem}[Main Theorem, splitting form; Cox Thm 9.2]
  \label{thm:main_splitting}
  \lean{PrimesXNY2.MainTheorem.prime_repr_iff_splits}
  \leanok
  A prime $p \nmid n$ is of the form $x^2 + n y^2$ iff $p$ splits completely in the
  ring class field.
\end{theorem}
\begin{proof}
  \uses{thm:form_ideal_bridge, thm:artin_reciprocity, def:splits_completely, thm:chebotarev}
\end{proof}

\begin{theorem}[Main Theorem, class polynomial form; Cox Cor 9.4]
  \label{thm:main_class_poly}
  \lean{PrimesXNY2.MainTheorem.prime_repr_iff_classPoly_root}
  \leanok
  $p = x^2 + n y^2$ iff $(-n/p) = 1$ and $f_n(x) \equiv 0 \pmod p$ has a root.
\end{theorem}
\begin{proof}
  \uses{thm:main_splitting, def:class_polynomial}
\end{proof}

\chapter{Part III — Complex Multiplication}

The deepest part: elliptic functions, the $j$-invariant, modular functions for
$\mathrm{SL}_2(\mathbb Z)$, complex multiplication, and Weber functions with
explicit class polynomials and Shimura reciprocity (2nd ed.). Almost entirely
to be formalized; see ROADMAP.md.

\section{Elliptic functions and the j-invariant}

\begin{definition}[Lattice]
  \label{def:lattice}
  \lean{PrimesXNY2.Elliptic.Lattice}
  \leanok
  A lattice $L = \mathbb Z\omega_1 + \mathbb Z\omega_2 \subseteq \mathbb C$.
\end{definition}

\begin{definition}[Weierstrass $\wp$]
  \label{def:weierstrass_p}
  \lean{PrimesXNY2.Elliptic.weierstrassP}
  \leanok
  \uses{def:lattice}
  The Weierstrass $\wp$-function of a lattice $L$.
\end{definition}

\begin{definition}[$j$-invariant of a lattice]
  \label{def:j_invariant_lattice}
  \lean{PrimesXNY2.Elliptic.jInvariant}
  \leanok
  \uses{def:lattice}
  $j(L) = 1728\, g_2^3 / (g_2^3 - 27 g_3^2)$.
\end{definition}

\begin{theorem}[Weierstrass differential equation]
  \label{thm:weierstrass_ode}
  \lean{PrimesXNY2.Elliptic.weierstrassP_differential_eq}
  \leanok
  $(\wp')^2 = 4\wp^3 - g_2 \wp - g_3$.
\end{theorem}
\begin{proof}
  \uses{def:weierstrass_p}
\end{proof}

\section{Modular functions and complex multiplication}

\begin{definition}[The $j$-function]
  \label{def:j_function}
  \lean{PrimesXNY2.Modular.jFunction}
  \leanok
  The Klein $j$-function $j(\tau)$ on the upper half-plane.
\end{definition}

\begin{definition}[Modular function]
  \label{def:modular_function}
  \lean{PrimesXNY2.Modular.IsModularFunction}
  \leanok
  A modular function for $\mathrm{SL}_2(\mathbb Z)$.
\end{definition}

\begin{theorem}[$j$ is modular]
  \label{thm:j_modular}
  \lean{PrimesXNY2.Modular.jFunction_modular}
  \leanok
  \uses{def:j_function}
  $j(\gamma\tau) = j(\tau)$ for $\gamma \in \mathrm{SL}_2(\mathbb Z)$.
\end{theorem}
\begin{proof}
  \uses{def:modular_function}
\end{proof}

\begin{definition}[CM point]
  \label{def:cm_point}
  \lean{PrimesXNY2.Modular.IsCMPoint}
  \leanok
  An imaginary quadratic $\tau$ (a complex multiplication point).
\end{definition}

\begin{theorem}[CM values are integral; Cox Thm 11.1]
  \label{thm:cm_integral}
  \lean{PrimesXNY2.Modular.isIntegral_jFunction_of_cm}
  \leanok
  If $\tau$ is a CM point then $j(\tau)$ is an algebraic integer.
\end{theorem}
\begin{proof}
  \uses{def:cm_point, def:j_function}
\end{proof}

\begin{theorem}[$j$ generates the ring class field; Cox Thm 11.1]
  \label{thm:j_generates}
  \lean{PrimesXNY2.Modular.jFunction_generates_ringClassField}
  \leanok
  There is a CM point whose $j$-value is an algebraic integer generating the ring
  class field $L = K(j(\tau))$.
\end{theorem}
\begin{proof}
  \uses{thm:cm_integral, def:ring_class_field}
\end{proof}

\section{Weber functions and explicit class polynomials}

\begin{definition}[Weber function]
  \label{def:weber_function}
  \lean{PrimesXNY2.Weber.weber}
  \leanok
  A Weber modular function $\mathfrak f(\tau)$.
\end{definition}

\begin{definition}[Weber class polynomial]
  \label{def:weber_class_polynomial}
  \lean{PrimesXNY2.Weber.weberClassPolynomial}
  \leanok
  \uses{def:weber_function}
  The reduced class polynomial built from Weber values.
\end{definition}

\begin{theorem}[Weber matches Hilbert class polynomial mod $p$]
  \label{thm:weber_root}
  \lean{PrimesXNY2.Weber.weberClassPolynomial_root_iff}
  \leanok
  The Weber class polynomial has a root mod $p$ iff the Hilbert class polynomial
  does, so it may replace $f_n$ in the criterion.
\end{theorem}
\begin{proof}
  \uses{def:weber_class_polynomial, def:class_polynomial}
\end{proof}

\begin{theorem}[Shimura reciprocity; 2nd ed.]
  \label{thm:shimura}
  \lean{PrimesXNY2.Weber.shimuraReciprocity}
  \leanok
  The idele class group acts on CM values of modular functions, computing the
  Galois action on Weber values. \emph{Deep: cite or assume.}
\end{theorem}
\begin{proof}
  \uses{def:cm_point, thm:artin_reciprocity}
\end{proof}
